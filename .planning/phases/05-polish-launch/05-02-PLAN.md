---
phase: 05-polish-launch
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - projects/briefs/beads-and-bloom-website/.env.example
  - projects/briefs/beads-and-bloom-website/DEPLOYMENT.md
  - projects/briefs/beads-and-bloom-website/src/app/(store)/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/(store)/products/[slug]/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/layout.tsx
autonomous: true
requirements:
  - LAUNCH-DEPLOY
  - LAUNCH-META

must_haves:
  truths:
    - "A new developer can configure and deploy the site using only .env.example and DEPLOYMENT.md"
    - "All required environment variables are documented with comments explaining their purpose and where to get them"
    - "Homepage and product pages have proper OG image meta tags for Instagram/social sharing"
    - "No sensitive values are hardcoded in source code"
    - "Build completes successfully with all changes"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/.env.example"
      provides: "Complete environment variable documentation"
      contains: "STRIPE_SECRET_KEY"
    - path: "projects/briefs/beads-and-bloom-website/DEPLOYMENT.md"
      provides: "Step-by-step deployment guide"
      contains: "Vercel"
  key_links:
    - from: ".env.example"
      to: "all source files using process.env"
      via: "environment variable names"
      pattern: "process\\.env\\."
    - from: "DEPLOYMENT.md"
      to: ".env.example"
      via: "references env vars to configure"
      pattern: "\\.env"
---

<objective>
Create deployment documentation, comprehensive .env.example, and ensure social sharing meta tags are production-ready.

Purpose: The founders (or a helper) need a clear path to deploy the site to Vercel with all services configured. Instagram link shares must look good with proper OG images.
Output: .env.example, DEPLOYMENT.md, updated meta tags on key pages
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-launch/05-CONTEXT.md
@.planning/phases/05-polish-launch/05-01-SUMMARY.md

@projects/briefs/beads-and-bloom-website/src/app/layout.tsx
@projects/briefs/beads-and-bloom-website/src/app/(store)/page.tsx
@projects/briefs/beads-and-bloom-website/src/app/(store)/products/[slug]/page.tsx
@projects/briefs/beads-and-bloom-website/next.config.ts
@projects/briefs/beads-and-bloom-website/src/app/api/webhook/route.ts
@projects/briefs/beads-and-bloom-website/src/middleware.ts
@projects/briefs/beads-and-bloom-website/src/lib/stripe.ts
@projects/briefs/beads-and-bloom-website/src/db/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive .env.example and deployment guide</name>
  <files>
    projects/briefs/beads-and-bloom-website/.env.example
    projects/briefs/beads-and-bloom-website/DEPLOYMENT.md
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/app/api/webhook/route.ts
    projects/briefs/beads-and-bloom-website/src/middleware.ts
    projects/briefs/beads-and-bloom-website/src/lib/stripe.ts
    projects/briefs/beads-and-bloom-website/src/db/index.ts
    projects/briefs/beads-and-bloom-website/src/actions/admin-auth.ts
    projects/briefs/beads-and-bloom-website/src/components/checkout/stripe-checkout.tsx
    projects/briefs/beads-and-bloom-website/src/components/admin/product-form.tsx
    projects/briefs/beads-and-bloom-website/src/emails/order-confirmation.tsx
    projects/briefs/beads-and-bloom-website/src/emails/admin-notification.tsx
  </read_first>
  <action>
    Grep ALL process.env references in the project to build a complete list. Per D-13, D-14, D-15.

    **1. Create .env.example** with every env var used in the codebase, grouped by service:

    ```
    # ===========================================
    # Beads & Bloom Environment Variables
    # Copy this file to .env.local and fill in values
    # ===========================================

    # --- Database (Neon Postgres) ---
    # Get from: https://console.neon.tech > Project > Connection Details
    DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

    # --- Stripe ---
    # Get from: https://dashboard.stripe.com/apikeys
    STRIPE_SECRET_KEY=sk_test_...
    NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
    # Get from: https://dashboard.stripe.com/webhooks (after creating endpoint)
    STRIPE_WEBHOOK_SECRET=whsec_...

    # --- Cloudinary ---
    # Get from: https://console.cloudinary.com/settings
    NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
    NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your-unsigned-preset

    # --- Resend (Email) ---
    # Get from: https://resend.com/api-keys
    RESEND_API_KEY=re_...
    # The verified sender email (must verify domain in Resend dashboard)
    RESEND_FROM_EMAIL=orders@yourdomain.com
    # Email to receive admin order notifications
    ADMIN_EMAIL=your-email@example.com

    # --- Admin Auth ---
    # Choose a strong password for the admin dashboard
    ADMIN_PASSWORD=your-admin-password
    # Random string for session cookie signing (generate with: openssl rand -base64 32)
    ADMIN_SESSION_SECRET=random-secret-string

    # --- Site ---
    # Your production URL (no trailing slash)
    NEXT_PUBLIC_BASE_URL=https://yourdomain.com
    ```

    Verify completeness: grep ALL `process.env.` references in src/ and confirm every variable is in .env.example. If any are missing, add them.

    **2. Create DEPLOYMENT.md** -- per D-14:

    Write a step-by-step deployment guide covering:

    a. **Prerequisites**: Node.js 18+, npm, a Vercel account (Pro plan for commercial use -- $20/mo), accounts for Neon, Stripe, Cloudinary, Resend

    b. **Step 1: Database Setup (Neon)**
       - Create project at neon.tech
       - Copy connection string to DATABASE_URL
       - Run migrations: `npx drizzle-kit push`
       - Run seed: `npx tsx scripts/seed.ts`

    c. **Step 2: Stripe Setup**
       - Create account at stripe.com
       - Get API keys (test mode first, then live)
       - Note: Webhook URL configured after deployment

    d. **Step 3: Cloudinary Setup**
       - Create account at cloudinary.com
       - Get cloud name from dashboard
       - Create unsigned upload preset for admin product uploads

    e. **Step 4: Resend Setup**
       - Create account at resend.com
       - Generate API key
       - Add and verify sending domain (for SPF/DKIM/DMARC -- per D-14)
       - Note the required DNS records for email deliverability

    f. **Step 5: Deploy to Vercel**
       - Import Git repository
       - Set all environment variables from .env.example
       - Deploy
       - Custom domain setup

    g. **Step 6: Post-Deploy**
       - Create Stripe webhook pointing to https://yourdomain.com/api/webhook
       - Set STRIPE_WEBHOOK_SECRET from the webhook signing secret
       - Redeploy with webhook secret
       - Test: place a test order with Stripe test card 4242424242424242

    h. **Troubleshooting** section for common issues

    Also verify per D-15: grep for any hardcoded API keys, passwords, or secrets in the source. There should be none.
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && test -f .env.example && test -f DEPLOYMENT.md && echo "PASS: Both files exist"</automated>
  </verify>
  <acceptance_criteria>
    - grep "DATABASE_URL" .env.example confirms database config documented
    - grep "STRIPE_SECRET_KEY" .env.example confirms Stripe config documented
    - grep "RESEND_API_KEY" .env.example confirms Resend config documented
    - grep "ADMIN_PASSWORD" .env.example confirms admin auth documented
    - grep "NEXT_PUBLIC_BASE_URL" .env.example confirms base URL documented
    - grep "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME" .env.example confirms Cloudinary documented
    - grep "NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET" .env.example confirms upload preset documented
    - grep "Vercel" DEPLOYMENT.md confirms deployment platform documented
    - grep "webhook" DEPLOYMENT.md confirms webhook setup documented
    - grep -i "SPF\|DKIM\|deliverability\|DNS" DEPLOYMENT.md confirms email deliverability covered
  </acceptance_criteria>
  <done>.env.example lists every environment variable with comments and source URLs. DEPLOYMENT.md provides step-by-step guide covering all services. No hardcoded secrets in source code.</done>
</task>

<task type="auto">
  <name>Task 2: Social sharing meta tags and final build verification</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx
    projects/briefs/beads-and-bloom-website/src/app/(store)/page.tsx
    projects/briefs/beads-and-bloom-website/src/app/(store)/products/[slug]/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx
    projects/briefs/beads-and-bloom-website/src/app/(store)/page.tsx
    projects/briefs/beads-and-bloom-website/src/app/(store)/products/[slug]/page.tsx
  </read_first>
  <action>
    Audit and enhance social sharing meta tags on key pages. Per D-16.

    **1. Root layout.tsx** -- Update the existing metadata export:
    - Ensure `metadataBase` is set: `metadataBase: new URL(process.env.NEXT_PUBLIC_BASE_URL || "https://beadsandbloom.com")`
    - Add `openGraph` defaults:
      ```
      openGraph: {
        type: "website",
        siteName: "Beads & Bloom",
        locale: "en_US",
      }
      ```
    - Add `twitter` card defaults:
      ```
      twitter: {
        card: "summary_large_image",
      }
      ```
    - Keep existing title and description

    **2. Homepage (store)/page.tsx** -- Check if generateMetadata or metadata export exists:
    - If no metadata export, add one with:
      - title: "Beads & Bloom -- Handmade Ocean-Inspired Jewelry"
      - description: (the existing one from root layout is fine as fallback)
      - openGraph.images: use a product image from Cloudinary as the OG image (construct URL like `https://res.cloudinary.com/${process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME}/image/upload/w_1200,h_630,c_fill/{first_product_image}` -- or use a static hero image if one exists)
    - If metadata already exists, ensure openGraph.images is set

    **3. Product detail page** -- Check existing generateMetadata:
    - The product detail page likely already has generateMetadata (Phase 2 added SEO per STORE-08)
    - Verify it includes openGraph.images using the product's first image from Cloudinary
    - If openGraph.images is missing, add it using: `https://res.cloudinary.com/${process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME}/image/upload/w_1200,h_630,c_fill/${product.images[0]}`
    - Ensure twitter.card is set to "summary_large_image"

    **4. Final build verification:**
    - Run `next build` and confirm it passes
    - Check for any TypeScript errors or warnings
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node ../../node_modules/.bin/next build 2>&1 | tail -20 || node node_modules/.bin/next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "metadataBase" src/app/layout.tsx confirms base URL for meta tags is set
    - grep "openGraph" src/app/layout.tsx confirms OG defaults in root layout
    - grep "twitter" src/app/layout.tsx confirms Twitter card defaults in root layout
    - grep -l "openGraph\|og" src/app/\(store\)/products/\[slug\]/page.tsx confirms product pages have OG tags
    - Build completes successfully
  </acceptance_criteria>
  <done>Root layout has metadataBase, openGraph defaults, and twitter card config. Product pages have OG images for social sharing. Homepage has OG image. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. .env.example contains ALL environment variables used in the project (cross-reference with grep)
2. DEPLOYMENT.md covers all services: Neon, Stripe, Cloudinary, Resend, Vercel
3. Root layout.tsx has metadataBase, openGraph, and twitter metadata
4. Product detail pages have OG images from Cloudinary
5. Final `next build` passes with zero errors
</verification>

<success_criteria>
- .env.example is comprehensive with comments and source URLs for every variable
- DEPLOYMENT.md is a complete step-by-step guide a new person could follow
- Social sharing meta tags produce proper previews when Instagram links are shared
- No hardcoded secrets in source code
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-launch/05-02-SUMMARY.md`
</output>
