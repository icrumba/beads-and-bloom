---
phase: 04-admin-order-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - projects/briefs/beads-and-bloom-website/src/app/admin/orders/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/admin/orders/[id]/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/admin/customers/page.tsx
  - projects/briefs/beads-and-bloom-website/src/components/admin/order-table.tsx
  - projects/briefs/beads-and-bloom-website/src/components/admin/order-status-badge.tsx
  - projects/briefs/beads-and-bloom-website/src/components/admin/order-status-button.tsx
  - projects/briefs/beads-and-bloom-website/src/actions/orders.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-05

must_haves:
  truths:
    - "Founders can see a list of all orders with customer name, date, total, and status badge"
    - "Founders can filter orders by status tab (All, New, Making, Shipped, Delivered)"
    - "Founders can search orders by customer name or order number"
    - "Founders can tap 'Next Status' to advance an order one step forward"
    - "Status badges show correct colors: New (blue), Making (amber), Shipped (purple), Delivered (green)"
    - "Order detail page shows full order info including items, customer contact, shipping address, gift message"
    - "Customers page shows contact details, shipping address, order count"
  artifacts:
    - path: "src/app/admin/orders/page.tsx"
      provides: "Order list page"
      contains: "OrderTable"
    - path: "src/components/admin/order-table.tsx"
      provides: "Data table with filters"
      contains: "useReactTable"
    - path: "src/components/admin/order-status-badge.tsx"
      provides: "Color-coded status badges"
      contains: "statusConfig"
    - path: "src/components/admin/order-status-button.tsx"
      provides: "One-tap status advance"
      contains: "advanceOrderStatus"
    - path: "src/actions/orders.ts"
      provides: "Order status mutation"
      contains: "use server"
    - path: "src/app/admin/orders/[id]/page.tsx"
      provides: "Order detail page"
      contains: "getOrderDetail"
    - path: "src/app/admin/customers/page.tsx"
      provides: "Customer list page"
      contains: "getAdminCustomers"
  key_links:
    - from: "src/components/admin/order-status-button.tsx"
      to: "src/actions/orders.ts"
      via: "advanceOrderStatus server action call"
      pattern: "advanceOrderStatus"
    - from: "src/app/admin/orders/page.tsx"
      to: "src/lib/queries.ts"
      via: "getAdminOrders query"
      pattern: "getAdminOrders"
    - from: "src/app/admin/orders/[id]/page.tsx"
      to: "src/lib/queries.ts"
      via: "getOrderDetail query"
      pattern: "getOrderDetail"
---

<objective>
Build the order management interface: order list with data table, filtering/search, one-tap status advance, order detail page, and customer list page.

Purpose: This is the core admin functionality -- founders need to see incoming orders and advance them through the fulfillment pipeline (New -> Making -> Shipped -> Delivered) from their phones.
Output: Working order list with @tanstack/react-table, status badges, one-tap advance, order detail page, and customers page.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-order-management/04-CONTEXT.md
@.planning/phases/04-admin-order-management/04-RESEARCH.md
@.planning/phases/04-admin-order-management/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: admin queries added to queries.ts -->

From src/lib/queries.ts (added in Plan 01):
```typescript
export async function getAdminOrders(status?: string): Promise<{
  id: number; status: string; totalAmount: string; giftMessage: string | null;
  createdAt: Date; customerName: string; customerEmail: string;
}[]>;

export async function getOrderDetail(orderId: number): Promise<{
  ...Order; items: { id: number; quantity: number; unitPrice: string;
  customColors: string[] | null; productName: string; productImages: string[] }[];
  customer: Customer | null;
} | null>;

export async function getAdminCustomers(): Promise<Customer[]>;
```

From src/types/index.ts:
```typescript
export type OrderStatus = "new" | "confirmed" | "making" | "shipped" | "delivered";
export type Order = InferSelectModel<typeof orders>;
export type Customer = InferSelectModel<typeof customers>;
export type Address = { line1: string; line2?: string; city: string; state: string; zip: string; country: string; };
```

From src/components/admin/admin-nav.tsx (created in Plan 01):
- Top nav bar with Orders, Products, Customers links (already in admin layout)

From src/components/ui/ (installed in Plan 01):
- Table, Input, Badge, Card, Tabs, Button, Separator, Skeleton available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order list page with data table, status badges, filtering, search, and one-tap advance</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/admin/orders/page.tsx,
    projects/briefs/beads-and-bloom-website/src/components/admin/order-table.tsx,
    projects/briefs/beads-and-bloom-website/src/components/admin/order-status-badge.tsx,
    projects/briefs/beads-and-bloom-website/src/components/admin/order-status-button.tsx,
    projects/briefs/beads-and-bloom-website/src/actions/orders.ts
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/db/schema.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/app/admin/layout.tsx
  </read_first>
  <action>
    **Step 1: Create order status badge component** (per D-05)
    Create `src/components/admin/order-status-badge.tsx`:
    - Uses shadcn Badge component
    - Status-to-color mapping:
      - new: `bg-blue-100 text-blue-700 hover:bg-blue-100` label "New"
      - making: `bg-amber-100 text-amber-700 hover:bg-amber-100` label "Making"
      - shipped: `bg-purple-100 text-purple-700 hover:bg-purple-100` label "Shipped"
      - delivered: `bg-green-100 text-green-700 hover:bg-green-100` label "Delivered"
      - confirmed: `bg-blue-100 text-blue-700 hover:bg-blue-100` label "Confirmed" (fallback, treated like new)
    - Props: `{ status: string }`

    **Step 2: Create advanceOrderStatus Server Action** (per D-06, ADMIN-02)
    Create `src/actions/orders.ts` with `"use server"` directive:
    - Import `z` from `"zod/v4"` (per existing pattern)
    - Define `statusFlow = ["new", "making", "shipped", "delivered"] as const`
    - `advanceOrderStatus(orderId: number)`:
      1. Fetch current order status from DB
      2. Find current index in statusFlow. If status is "confirmed", treat as index 0 (same as "new") per research Pitfall 7
      3. If already "delivered" (last status), return `{ error: "Order is already delivered" }`
      4. Set next status = statusFlow[currentIdx + 1]
      5. `await db.update(orders).set({ status: nextStatus, updatedAt: new Date() }).where(eq(orders.id, orderId))`
      6. `revalidatePath("/admin/orders")` AND `revalidatePath("/admin/orders/" + orderId)`
      7. Return `{ success: true, newStatus: nextStatus }`

    **Step 3: Create order status button component** (per D-06)
    Create `src/components/admin/order-status-button.tsx` as a Client Component:
    - Props: `{ orderId: number; currentStatus: string }`
    - If currentStatus is "delivered", render nothing (or disabled text "Delivered")
    - Otherwise render a small Button that shows the NEXT status label (e.g., if "new" -> "Start Making", if "making" -> "Mark Shipped", if "shipped" -> "Mark Delivered")
    - On click, call `advanceOrderStatus(orderId)` and show toast via `sonner`: `toast.success("Status updated to ${result.newStatus}")` (per D-09)
    - Show loading spinner while action is pending (use `useTransition` from React)
    - Button is compact enough for mobile tap targets (min 44px height)

    **Step 4: Create order data table component** (per D-04, D-08)
    Create `src/components/admin/order-table.tsx` as a Client Component:
    - Uses `@tanstack/react-table` with `useReactTable`, `getCoreRowModel`, `getFilteredRowModel`, `getSortedRowModel`
    - Uses shadcn Table component for rendering
    - Props: `{ data: AdminOrder[]; initialStatus?: string }` where AdminOrder matches the getAdminOrders return type
    - Column definitions (per D-04):
      1. Order # (`id`): display as `#${id}`, link to `/admin/orders/${id}`
      2. Customer (`customerName`): plain text
      3. Date (`createdAt`): formatted with date-fns `format(date, "MMM d, yyyy")`
      4. Total (`totalAmount`): display as `$${value}` (already formatted as string like "6.00")
      5. Status: render `<OrderStatusBadge status={status} />`
      6. Actions: render `<OrderStatusButton orderId={id} currentStatus={status} />`
    - Status tab filtering (per D-08): Row of tabs above table -- "All", "New", "Making", "Shipped", "Delivered". Clicking a tab filters the table client-side by status. Use shadcn Tabs component or simple button group.
    - Search (per D-08): Input field above table that filters by customerName or id (order number). Use `@tanstack/react-table` column filtering or a simple state filter.
    - Client-side pagination: 20 rows per page (sufficient for low volume). Use `getPaginationRowModel()`.
    - Mobile responsive: on small screens, hide the Date column. Keep Order #, Customer, Status, and Actions visible. Use `@media` or Tailwind responsive classes.

    **Step 5: Create orders list page** (per D-04, ADMIN-01)
    Create `src/app/admin/orders/page.tsx` as a Server Component:
    - Metadata: `title: "Orders | Admin"`
    - Fetch orders: `const orders = await getAdminOrders()`
    - Render heading "Orders" + `<OrderTable data={orders} />`
    - `export const dynamic = "force-dynamic"` to always fetch fresh data
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/.bin/next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "useReactTable" src/components/admin/order-table.tsx confirms data table uses @tanstack/react-table
    - grep -l "statusConfig" src/components/admin/order-status-badge.tsx confirms badge has color mapping
    - grep -l "advanceOrderStatus" src/actions/orders.ts confirms status advance action exists
    - grep -l "revalidatePath" src/actions/orders.ts confirms cache invalidation after mutation
    - grep -l "toast" src/components/admin/order-status-button.tsx confirms sonner toast on status update
    - grep -l "getAdminOrders" src/app/admin/orders/page.tsx confirms page fetches orders
    - grep -l "OrderTable" src/app/admin/orders/page.tsx confirms page renders data table
  </acceptance_criteria>
  <done>Order list page at /admin/orders shows all orders in a data table with columns: order number (linked to detail), customer name, date, total, status badge (color-coded per D-05), and one-tap advance button. Status tabs filter by All/New/Making/Shipped/Delivered. Search filters by customer name or order number. Status advance uses Server Action with sonner toast confirmation. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Order detail page and customers page</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/admin/orders/[id]/page.tsx,
    projects/briefs/beads-and-bloom-website/src/app/admin/customers/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/components/admin/order-status-badge.tsx,
    projects/briefs/beads-and-bloom-website/src/components/admin/order-status-button.tsx,
    projects/briefs/beads-and-bloom-website/src/db/schema.ts
  </read_first>
  <action>
    **Step 1: Create order detail page** (per D-07, ADMIN-05)
    Create `src/app/admin/orders/[id]/page.tsx` as a Server Component:
    - Metadata: dynamic title `"Order #${id} | Admin"`
    - Parse `params.id` as integer. If invalid or order not found, `notFound()`.
    - Fetch: `const order = await getOrderDetail(parseInt(params.id))`
    - Layout: mobile-friendly single column with Card sections:
      1. **Header**: "Order #{id}" with OrderStatusBadge and OrderStatusButton
      2. **Items section**: List each item with product name, quantity, unit price, custom colors (if any). Show product image thumbnail via CldImage if available (first image from productImages array). Total at bottom.
      3. **Customer section**: Name, email (clickable mailto link), phone (clickable tel link if present). Shows order count ("3rd order" etc.) per ADMIN-05.
      4. **Shipping section**: Full address formatted on multiple lines.
      5. **Gift message section** (only if present): Styled quote box (teal background like order confirmation email).
      6. **Order info section**: Order date (formatted), Stripe session ID (truncated, for reference), payment status.
    - Back link to `/admin/orders`
    - `export const dynamic = "force-dynamic"`

    **Step 2: Create customers page** (per ADMIN-05)
    Create `src/app/admin/customers/page.tsx` as a Server Component:
    - Metadata: `title: "Customers | Admin"`
    - Fetch: `const customerList = await getAdminCustomers()`
    - Display as a simple card list (not a full data table -- customer volume is low):
      - Each customer in a Card: name (bold), email (clickable), phone, address (if present), order count, member since date
      - Cards stack vertically, mobile-friendly
      - If no customers yet, show empty state: "No customers yet. Orders will appear here."
    - Sort by most recent first (already sorted by `desc(createdAt)` in query)
    - `export const dynamic = "force-dynamic"`
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/.bin/next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "getOrderDetail" src/app/admin/orders/\[id\]/page.tsx confirms detail page fetches full order
    - grep -l "OrderStatusBadge" src/app/admin/orders/\[id\]/page.tsx confirms detail page shows status badge
    - grep -l "OrderStatusButton" src/app/admin/orders/\[id\]/page.tsx confirms detail page has status advance
    - grep -l "giftMessage" src/app/admin/orders/\[id\]/page.tsx confirms gift message display
    - grep -l "getAdminCustomers" src/app/admin/customers/page.tsx confirms customers page fetches data
    - grep -l "orderCount" src/app/admin/customers/page.tsx confirms customer order history shown
  </acceptance_criteria>
  <done>Order detail page at /admin/orders/[id] shows full order info: items with images and custom colors, customer contact details with clickable links, shipping address, gift message, order date, and status controls. Customers page at /admin/customers shows all customers with contact info, address, and order count. Both pages are mobile-friendly and use force-dynamic for fresh data. Build passes.</done>
</task>

</tasks>

<verification>
1. `node node_modules/.bin/next build` completes without errors
2. Order list page renders at /admin/orders with data table
3. Status badges use correct colors per D-05
4. One-tap advance button works via Server Action
5. Order detail page shows all order information per D-07
6. Customers page shows customer info per ADMIN-05
7. All pages are force-dynamic for fresh data
</verification>

<success_criteria>
- /admin/orders shows all orders in a filterable, searchable data table with status badges and one-tap advance
- /admin/orders/[id] shows complete order details including items, customer, shipping, gift message
- /admin/customers shows all customers with contact info and order history
- Status advance works: New -> Making -> Shipped -> Delivered with toast confirmation
- All pages are mobile-friendly (single column, large tap targets)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-order-management/04-02-SUMMARY.md`
</output>
