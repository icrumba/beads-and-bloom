---
phase: 02-storefront-brand
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - projects/briefs/beads-and-bloom-website/src/app/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/products/[slug]/page.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/product-card.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/product-grid.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/category-tabs.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/photo-carousel.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/availability-badge.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/hero-section.tsx
autonomous: true
requirements: [STORE-01, STORE-02, STORE-08, STORE-10]

must_haves:
  truths:
    - "User can browse products in a 2-column mobile grid that expands to 3 on tablet and 4 on desktop"
    - "User can filter products by category using tabs (All, Bracelets, Necklaces, Accessories)"
    - "Featured product displays large on top of the homepage grid"
    - "User can tap a product card to view the detail page"
    - "Product detail page shows swipeable photo carousel, name, price, description, materials, and care info"
    - "Products display 'Ready to ship' or 'Made to order (5-7 days)' badges"
    - "Product detail pages have correct SEO title and description"
    - "Color swatches appear as small dots on product cards"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/src/app/page.tsx"
      provides: "Homepage with hero, category tabs, featured product, product grid"
      contains: "getProducts"
    - path: "projects/briefs/beads-and-bloom-website/src/app/products/[slug]/page.tsx"
      provides: "Product detail page with generateMetadata"
      contains: "generateMetadata"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/product-card.tsx"
      provides: "Product card with photo, name, price, badge, swatches"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/product-grid.tsx"
      provides: "Mixed layout grid (featured large + 2-col grid)"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/category-tabs.tsx"
      provides: "Client-side category filter tabs"
      contains: "use client"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/photo-carousel.tsx"
      provides: "CSS scroll-snap photo carousel with dot indicators"
      contains: "scroll-snap"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/availability-badge.tsx"
      provides: "Ready to ship / Made to order badge"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx"
      provides: "Small color dots for product cards"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/queries.ts"
      via: "Server Component import"
      pattern: "getProducts|getFeaturedProducts"
    - from: "src/app/page.tsx"
      to: "src/components/shop/product-grid.tsx"
      via: "renders ProductGrid with products prop"
      pattern: "ProductGrid"
    - from: "src/components/shop/product-card.tsx"
      to: "/products/[slug]"
      via: "Next.js Link wrapping entire card"
      pattern: "href.*products/"
    - from: "src/app/products/[slug]/page.tsx"
      to: "src/lib/queries.ts"
      via: "getProductBySlug for data and metadata"
      pattern: "getProductBySlug"
---

<objective>
Product catalog -- the browsing experience customers use when arriving from Instagram.

Purpose: This is the core shopping flow. Users browse the grid, filter by category, tap into product details with photos and info. The homepage and product detail page are the two most-visited pages.

Output: Fully functional homepage with hero, category tabs, featured product, product grid. Product detail pages with photo carousel, all product info, and SEO metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storefront-brand/02-CONTEXT.md
@.planning/phases/02-storefront-brand/02-UI-SPEC.md
@.planning/phases/02-storefront-brand/02-RESEARCH.md
@.planning/phases/02-storefront-brand/02-01-SUMMARY.md

@projects/briefs/beads-and-bloom-website/src/db/schema.ts
@projects/briefs/beads-and-bloom-website/src/types/index.ts
@projects/briefs/beads-and-bloom-website/src/lib/queries.ts
@projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx
@projects/briefs/beads-and-bloom-website/src/app/layout.tsx
@projects/briefs/beads-and-bloom-website/src/app/globals.css

<interfaces>
<!-- From Plan 01 outputs -- executor uses these directly -->

From src/lib/queries.ts (created by Plan 01):
```typescript
export async function getProducts(category?: string): Promise<Product[]>;
export async function getProductBySlug(slug: string): Promise<Product | null>;
export async function getFeaturedProducts(): Promise<Product[]>;
export async function getCharityTotal(): Promise<CharityTotal>;
```

From src/types/index.ts:
```typescript
export type Product = {
  id: number;
  name: string;
  slug: string;
  description: string;
  price: string;  // numeric(10,2) returns string
  category: string;
  images: string[];  // Cloudinary public IDs
  colors: string[];  // hex color codes
  customizable: boolean;
  availability: "ready_to_ship" | "made_to_order";
  inStock: boolean;
  featured: boolean;
  materials: string | null;
  careInfo: string | null;
  sortOrder: number;
  createdAt: Date;
  updatedAt: Date;
};
export type Availability = "ready_to_ship" | "made_to_order";
```

From shadcn components (installed by Plan 01):
- Card, CardContent, CardHeader from "@/components/ui/card"
- Badge from "@/components/ui/badge"
- Tabs, TabsList, TabsTrigger from "@/components/ui/tabs"
- Skeleton from "@/components/ui/skeleton"
- Separator from "@/components/ui/separator"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build homepage with hero section, category tabs, and product grid</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/page.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shared/hero-section.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/category-tabs.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/product-grid.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/product-card.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/availability-badge.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/db/schema.ts,
    projects/briefs/beads-and-bloom-website/src/app/globals.css,
    projects/briefs/beads-and-bloom-website/src/components/ui/card.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/badge.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/tabs.tsx,
    .planning/phases/02-storefront-brand/02-UI-SPEC.md,
    .planning/phases/02-storefront-brand/02-RESEARCH.md
  </read_first>
  <action>
    **Component 1: `src/components/shop/availability-badge.tsx` (server component)**
    - Props: `{ availability: "ready_to_ship" | "made_to_order" }`
    - Uses shadcn Badge component with `variant="secondary"`
    - "ready_to_ship" renders: "Ready to ship" with `text-muted-foreground bg-secondary` styling
    - "made_to_order" renders: "Made to order (5-7 days)" with same muted styling
    - Per UI-SPEC: subtle, not alarming. Use 14px font-size, semibold weight.
    - Badge should NOT use accent color -- per UI-SPEC accent is reserved for CTAs, active tabs, counter, and links only.

    **Component 2: `src/components/shop/color-swatches.tsx` (server component)**
    - Props: `{ colors: string[] }` (hex color codes from product.colors)
    - Renders small circular dots: 12px diameter, 4px gap between dots (per UI-SPEC)
    - Each dot: `rounded-full` with inline `backgroundColor` style using the hex color
    - Add 1px border `border border-border` so white/light colors are visible
    - If colors array is empty, render nothing (return null)
    - Max display: 5 swatches. If more than 5, show "+N" text after the 5th dot in 12px muted text.

    **Component 3: `src/components/shop/product-card.tsx` (client component -- needs CldImage)**
    - `"use client"` (CldImage requires client component)
    - Props: `{ product: Product, featured?: boolean }`
    - Entire card is wrapped in Next.js `Link` to `/products/${product.slug}`
    - Uses shadcn Card component
    - Card content (per D-02): CldImage photo, product name, price, AvailabilityBadge, ColorSwatches
    - Photo: CldImage with `crop="fill"`, `gravity="center"`, `format="auto"`, `quality="auto"`
      - Normal card: width={400} height={400}, sizes="(max-width: 768px) 50vw, (max-width: 1024px) 33vw, 25vw"
      - Featured card: width={800} height={600}, sizes="(max-width: 768px) 100vw, 50vw"
    - If `featured` is true: card spans full width, larger photo (800x600), name at 24px semibold, price at 16px
    - Normal card: name at 16px semibold, price at 14px semibold text-primary
    - Price display: `$${parseFloat(product.price).toFixed(2)}` (Drizzle numeric returns string)
    - Desktop hover: card lifts 2px with shadow transition -- `hover:shadow-md hover:-translate-y-0.5 transition-all duration-200` (per UI-SPEC interaction pattern). Use `@media (hover: hover)` or Tailwind `hover:` which auto-handles touch devices.
    - Card background: `bg-card` (sandy warm #F0EBE3 per CSS variables)
    - Padding: 8px inner card padding (sm spacing token), 8px gap between elements

    **Component 4: `src/components/shop/category-tabs.tsx` (client component, per D-03)**
    - `"use client"` directive
    - Props: `{ active: string }`
    - Uses shadcn Tabs with TabsList and TabsTrigger
    - Categories: `["all", "bracelets", "necklaces", "accessories"]`
    - Tab change updates URL search params: `?category=bracelets` (or removes param for "all")
    - Use `useRouter()` and `useSearchParams()` from `next/navigation`
    - Wrap in `Suspense` boundary where used (useSearchParams requires Suspense in Next.js 15)
    - Active tab: accent underline (handled by shadcn Tabs default styling with primary color)
    - Tab labels: capitalize first letter, 14px semibold (per UI-SPEC Label role)
    - TabsList should be horizontally scrollable on mobile if needed: `overflow-x-auto`

    **Component 5: `src/components/shop/product-grid.tsx` (server component)**
    - Props: `{ products: Product[], featured?: Product }`
    - Mixed layout per D-01: if `featured` exists, render it large on top (full width), then remaining products in grid below
    - Grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2 md:gap-4` (8px mobile / 16px tablet+)
    - Featured product renders ProductCard with `featured={true}`
    - Non-featured products render ProductCard with default sizing
    - If products array is empty, show empty state: "Nothing here yet!" heading + "We're working on new pieces for this collection. Check out our other jewelry in the meantime!" body (per UI-SPEC copy contract)
    - Empty state: centered text, heading 24px semibold, body 16px text-muted-foreground, 48px vertical padding

    **Component 6: `src/components/shared/hero-section.tsx` (server component)**
    - Per D-14: first section on homepage
    - Background: `bg-secondary` (sandy warm) with padding 48px vertical, 16px horizontal on mobile
    - Tagline (Display role): "Handmade with love. Every purchase gives back." -- 32px semibold, leading-tight
    - Subtitle (Body role): "Ocean-inspired jewelry by twin sisters -- $1 from every order goes to charity." -- 16px regular, text-muted-foreground, mt-3
    - No hero image for now (placeholder gradient background or solid sandy color) -- per RESEARCH open question #3, hero image may not exist yet. Use the solid `bg-secondary` background which is the sandy warm tone. Add a simple decorative wave SVG or just keep it clean.
    - Max-width 600px on the text content, centered on desktop
    - Mobile: left-aligned text, full width

    **Component 7: Replace `src/app/page.tsx` with full homepage (server component)**
    - This is a Server Component -- data fetched directly with Drizzle
    - Accept `searchParams` prop (Promise in Next.js 15 -- must `await searchParams`)
    - Fetch: `const { category } = await searchParams;`
    - Call `getProducts(category || "all")` and `getFeaturedProducts()`
    - Featured product = first item from `getFeaturedProducts()`, or first product if none featured
    - Layout order per D-14: HeroSection -> CategoryTabs (wrapped in Suspense) -> ProductGrid
    - Page padding: 16px horizontal on mobile (`px-4`), max-width 1200px centered on desktop (`max-w-[1200px] mx-auto`)
    - Section spacing: 48px between hero and tabs (`mt-12`), 24px between tabs and grid (`mt-6`)
    - Import Suspense from React for wrapping CategoryTabs
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx is a Server Component (no "use client") that imports getProducts and getFeaturedProducts
    - src/app/page.tsx awaits searchParams (Next.js 15 pattern)
    - src/components/shop/product-card.tsx contains "use client" and imports CldImage
    - src/components/shop/product-card.tsx wraps card in Link to `/products/${product.slug}`
    - src/components/shop/product-card.tsx displays parseFloat(product.price).toFixed(2)
    - src/components/shop/product-grid.tsx contains "grid grid-cols-2"
    - src/components/shop/product-grid.tsx renders empty state with "Nothing here yet!"
    - src/components/shop/category-tabs.tsx contains "use client" and uses useSearchParams
    - src/components/shop/category-tabs.tsx contains tabs for "all", "bracelets", "necklaces", "accessories"
    - src/components/shop/availability-badge.tsx renders "Ready to ship" or "Made to order (5-7 days)"
    - src/components/shop/color-swatches.tsx renders 12px diameter dots with border
    - src/components/shared/hero-section.tsx contains "Handmade with love. Every purchase gives back."
    - Build completes without errors
  </acceptance_criteria>
  <done>Homepage renders hero section, category tabs with URL-based filtering, featured product displayed large, and 2-column product grid on mobile. All product cards show photo, name, price, availability badge, and color swatches. Empty state displays when category has no products.</done>
</task>

<task type="auto">
  <name>Task 2: Build product detail page with photo carousel and SEO metadata</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/products/[slug]/page.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/photo-carousel.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/db/schema.ts,
    projects/briefs/beads-and-bloom-website/src/components/shop/availability-badge.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/separator.tsx,
    .planning/phases/02-storefront-brand/02-UI-SPEC.md,
    .planning/phases/02-storefront-brand/02-RESEARCH.md
  </read_first>
  <action>
    **Component 1: `src/components/shop/photo-carousel.tsx` (client component)**
    - `"use client"` directive
    - Props: `{ images: string[], alt: string }`
    - Uses CSS scroll-snap for swipeable photos (no library, per research recommendation)
    - Container: `flex overflow-x-auto snap-x snap-mandatory` with `scrollbar-width: none` and `-webkit-overflow-scrolling: touch`
    - Each image slide: `flex-none w-full snap-center`
    - Images: CldImage with width={800}, height={800}, `crop="fill"`, `format="auto"`, `quality="auto"`, sizes="100vw"
    - Dot indicators below the carousel:
      - Container: `flex justify-center gap-2 mt-3`
      - Active dot: 8px diameter (`w-2 h-2`), `rounded-full`, `bg-primary` (teal accent)
      - Inactive dot: 8px diameter, `bg-muted-foreground/30`
      - Track active index using IntersectionObserver on each slide (threshold: 0.5, root: carousel container)
    - If only 1 image, render single CldImage without carousel container or dots
    - If images array is empty, render a placeholder div with `bg-muted` and text "No photo available" centered

    **Component 2: `src/app/products/[slug]/page.tsx` (server component)**

    **Part A: `generateMetadata` function (per STORE-08)**
    ```typescript
    type Props = { params: Promise<{ slug: string }> };

    export async function generateMetadata({ params }: Props): Promise<Metadata> {
      const { slug } = await params;
      const product = await getProductBySlug(slug);
      if (!product) return {};
      const desc = `${product.description.slice(0, 155)}. Handmade with love. $1 donated to charity with every purchase.`;
      return {
        title: `${product.name} -- Beads & Bloom`,
        description: desc,
        openGraph: {
          title: `${product.name} -- Beads & Bloom`,
          description: desc,
          images: product.images[0]
            ? [`https://res.cloudinary.com/${process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME}/image/upload/w_1200,h_630,c_fill/${product.images[0]}`]
            : [],
        },
      };
    }
    ```

    **Part B: Page component**
    - Await params, call `getProductBySlug(slug)`, if null call `notFound()`
    - Layout per D-04:
      - Mobile (default): Photo carousel stacked on top, product info below
      - Tablet+ (md:): Side-by-side -- photos 50% left (`md:w-1/2`), info 50% right (`md:w-1/2`). Use `md:flex md:gap-8`.
      - Desktop (lg:): Photos 55% left (`lg:w-[55%]`), info 45% right (`lg:w-[45%]`)
    - Product info section (per D-05):
      - Name: 24px semibold (Heading role)
      - Price: 24px semibold, text-primary (teal) -- `$${parseFloat(product.price).toFixed(2)}`
      - AvailabilityBadge component (STORE-10)
      - ColorSwatches component (if product.colors.length > 0)
      - Separator
      - Description: 16px regular (Body role), mt-4
      - Materials section (if product.materials): heading "Materials" at 14px semibold, content at 16px regular text-muted-foreground
      - Care info section (if product.careInfo): heading "Care" at 14px semibold, content at 16px regular text-muted-foreground
      - Separator between materials and care
      - "Add to Cart" button: shadcn Button with `size="lg"`, full width on mobile (`w-full md:w-auto`), bg-primary (teal). Non-functional in Phase 02 -- just styled. Text: "Add to Cart" per UI-SPEC.
    - Page padding: 16px horizontal on mobile, max-width 1200px centered on desktop
    - Back navigation: Link to "/" with ChevronLeft icon from lucide-react, "Back to Shop" text, positioned above the product content
    - Page-level padding: 16px top from header
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/products/[slug]/page.tsx exports generateMetadata function
    - src/app/products/[slug]/page.tsx awaits params (Next.js 15 pattern)
    - src/app/products/[slug]/page.tsx calls getProductBySlug and notFound()
    - src/app/products/[slug]/page.tsx renders product name, price with parseFloat, description, materials, careInfo
    - src/app/products/[slug]/page.tsx renders AvailabilityBadge and ColorSwatches
    - src/app/products/[slug]/page.tsx contains "md:flex" for tablet side-by-side layout (D-04)
    - src/app/products/[slug]/page.tsx contains "Add to Cart" button text
    - src/components/shop/photo-carousel.tsx contains "use client" and "snap-x snap-mandatory"
    - src/components/shop/photo-carousel.tsx uses IntersectionObserver for dot indicators
    - src/components/shop/photo-carousel.tsx imports CldImage
    - generateMetadata returns title format "[Product Name] -- Beads & Bloom"
    - Build completes without errors
  </acceptance_criteria>
  <done>Product detail page renders all product info (photos, name, price, description, materials, care) with responsive layout (stacked mobile, side-by-side tablet+). Photo carousel swipes with dot indicators. SEO metadata generates dynamically per product. Availability badges and color swatches display correctly.</done>
</task>

</tasks>

<verification>
1. `cd projects/briefs/beads-and-bloom-website && node node_modules/next/dist/bin/next build` completes without errors
2. Homepage renders hero, category tabs, featured product, and product grid
3. Category tabs update URL search params
4. Product cards link to `/products/[slug]`
5. Product detail page renders all fields and has generateMetadata
6. Photo carousel uses CSS scroll-snap with dot indicators
7. Availability badges display correct text per product
8. Color swatches render as small circular dots
</verification>

<success_criteria>
- Users can browse products in a responsive grid with category filtering (STORE-01)
- Users can view complete product detail pages with all info (STORE-02)
- Products display correct availability flags (STORE-10)
- Product pages have dynamic SEO metadata (STORE-08)
- Build passes clean with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-storefront-brand/02-02-SUMMARY.md`
</output>
