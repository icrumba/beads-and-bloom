---
phase: 02-storefront-brand
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - projects/briefs/beads-and-bloom-website/src/app/about/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/contact/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/shipping/page.tsx
  - projects/briefs/beads-and-bloom-website/src/app/returns/page.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/charity-counter.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/instagram-gallery.tsx
  - projects/briefs/beads-and-bloom-website/src/app/page.tsx
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04, STORE-06, STORE-07, STORE-08]

must_haves:
  truths:
    - "About page tells the founder story with alternating photo/text sections"
    - "Contact page displays email address and Instagram DM link"
    - "Shipping page shows flat-rate shipping information"
    - "Returns page shows return/exchange policy"
    - "Homepage charity counter animates from 0 to the database total on scroll"
    - "Instagram gallery section on homepage links to Instagram profile"
    - "All brand pages have correct SEO metadata"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/src/app/about/page.tsx"
      provides: "Founder story with alternating photo/text layout"
      contains: "Our Story"
    - path: "projects/briefs/beads-and-bloom-website/src/app/contact/page.tsx"
      provides: "Contact page with email and Instagram"
      contains: "Say Hi"
    - path: "projects/briefs/beads-and-bloom-website/src/app/shipping/page.tsx"
      provides: "Flat-rate shipping information page"
      contains: "Shipping Info"
    - path: "projects/briefs/beads-and-bloom-website/src/app/returns/page.tsx"
      provides: "Return and exchange policy page"
      contains: "Returns"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shared/charity-counter.tsx"
      provides: "Animated count-up charity donation counter"
      contains: "use client"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shared/instagram-gallery.tsx"
      provides: "Curated image gallery linking to Instagram"
      contains: "instagram"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/shared/charity-counter.tsx"
      via: "Rendered on homepage with total from getCharityTotal()"
      pattern: "CharityCounter"
    - from: "src/app/page.tsx"
      to: "src/components/shared/instagram-gallery.tsx"
      via: "Rendered on homepage"
      pattern: "InstagramGallery"
    - from: "src/components/shared/charity-counter.tsx"
      to: "IntersectionObserver"
      via: "Browser API triggers animation on scroll"
      pattern: "IntersectionObserver"
---

<objective>
Brand storytelling pages and homepage charity/Instagram sections.

Purpose: These pages build trust and emotional connection. The about page tells the founder story, the charity counter shows impact, and the contact/shipping/returns pages answer practical questions. All critical for converting Instagram visitors into customers.

Output: About page with founder story, contact page, shipping info page, returns policy page, animated charity counter on homepage, Instagram gallery on homepage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storefront-brand/02-CONTEXT.md
@.planning/phases/02-storefront-brand/02-UI-SPEC.md
@.planning/phases/02-storefront-brand/02-RESEARCH.md
@.planning/phases/02-storefront-brand/02-01-SUMMARY.md

@projects/briefs/beads-and-bloom-website/src/lib/queries.ts
@projects/briefs/beads-and-bloom-website/src/types/index.ts
@projects/briefs/beads-and-bloom-website/src/app/globals.css
@projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx
@projects/briefs/beads-and-bloom-website/src/components/shared/footer.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/queries.ts:
```typescript
export async function getCharityTotal(): Promise<{ totalDonated: string; orderCount: number; }>;
```

CSS Variables (from globals.css after Plan 01):
```css
--primary: oklch(0.63 0.09 175);          /* #3B9B8F teal - for counter number */
--secondary: oklch(0.94 0.012 80);         /* #F0EBE3 sandy - for section backgrounds */
--muted-foreground: oklch(0.48 0 0);       /* #6B6B6B - for secondary text */
--background: oklch(0.98 0.002 100);       /* #FAFAF8 off-white */
```

Typography scale (from UI-SPEC):
- Display: 32px, semibold (600), line-height 1.1 -- charity counter number
- Heading: 24px, semibold (600), line-height 1.2 -- page titles
- Body: 16px, regular (400), line-height 1.5 -- descriptions
- Label: 14px, semibold (600), line-height 1.4 -- section subheadings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build about, contact, shipping, and returns pages with SEO metadata</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/about/page.tsx,
    projects/briefs/beads-and-bloom-website/src/app/contact/page.tsx,
    projects/briefs/beads-and-bloom-website/src/app/shipping/page.tsx,
    projects/briefs/beads-and-bloom-website/src/app/returns/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    projects/briefs/beads-and-bloom-website/src/app/globals.css,
    .planning/phases/02-storefront-brand/02-UI-SPEC.md,
    .planning/phases/02-storefront-brand/02-CONTEXT.md
  </read_first>
  <action>
    All four pages are Server Components (no "use client"). All use hardcoded JSX with Tailwind (not MDX -- per research recommendation for static pages).

    **Page 1: `src/app/about/page.tsx` (BRAND-01, per D-06)**

    SEO metadata (STORE-08):
    ```typescript
    export const metadata: Metadata = {
      title: "Our Story -- Beads & Bloom",
      description: "Meet the 13-year-old twin sisters behind Beads & Bloom. Handmade ocean-inspired jewelry that gives back with every purchase.",
    };
    ```

    Content structure -- alternating photo/text sections (per D-06):
    - Page heading: "Our Story" -- 24px semibold, centered on mobile, left-aligned on desktop
    - Mission line: "Making the world more beautiful -- one bracelet at a time." -- 16px, text-muted-foreground, mt-2
    - Section 1 (text left, image right on desktop): Story intro -- twin teen sisters who started making ocean-inspired jewelry. Text about how it started.
    - Section 2 (image left, text right on desktop): The craft -- close-up of making jewelry, details about materials and process.
    - Section 3 (text left, image right on desktop): The charity mission -- "Every bracelet we sell donates $1 to charity. Because giving back shouldn't have to wait until you're older." (per UI-SPEC copy)
    - Section 4 (image left, text right on desktop): The ocean connection -- inspiration from the ocean, sea turtles, starfish, shells.

    Layout:
    - Mobile: single column, full-width placeholder images (bg-muted rounded-lg aspect-[3/2]) alternating with text blocks
    - Desktop (md:): alternating side-by-side layout using `md:flex md:flex-row-reverse` for odd sections
    - Each section: 48px vertical spacing (`py-12`)
    - Text sections: max-width 500px
    - Image placeholders: Use CldImage if Cloudinary public IDs are defined, otherwise use `div` with `bg-secondary rounded-lg` as placeholder with text "Photo coming soon" centered
    - For now, use placeholder divs since about page photos may not be in Cloudinary yet (per research open question #1). Make them easy to swap with CldImage later.
    - Page padding: 16px horizontal on mobile (`px-4`), max-width 800px centered (`max-w-3xl mx-auto`)

    **Page 2: `src/app/contact/page.tsx` (BRAND-03)**

    SEO metadata:
    ```typescript
    export const metadata: Metadata = {
      title: "Contact Us -- Beads & Bloom",
      description: "Questions about our handmade jewelry? Want a custom color combo? Reach out via email or Instagram DM.",
    };
    ```

    Content:
    - Heading: "Say Hi!" -- 24px semibold (per UI-SPEC copy contract)
    - Body: "Got a question? Want a custom color combo? We'd love to hear from you!" -- 16px regular, text-muted-foreground
    - Contact methods in a clean card layout (bg-card rounded-lg p-6):
      - Email: lucide-react Mail icon (20x20) + "beadsandbloom@email.com" (placeholder email -- use mailto: link)
      - Instagram: lucide-react Instagram icon (20x20) + "DM us on Instagram @beadsandbloom" linking to `https://instagram.com/beadsandbloom` (target="_blank", rel="noopener noreferrer")
    - Each contact method: flex row with icon, 8px gap, 16px text
    - Card spacing: 24px between cards (or a single card with separator between methods)
    - Page padding: 16px horizontal, max-width 600px centered, 48px top padding

    **Page 3: `src/app/shipping/page.tsx` (STORE-06)**

    SEO metadata:
    ```typescript
    export const metadata: Metadata = {
      title: "Shipping Info -- Beads & Bloom",
      description: "Flat-rate shipping on all Beads & Bloom orders. Learn about our shipping times and delivery details.",
    };
    ```

    Content:
    - Heading: "Shipping Info" -- 24px semibold (per UI-SPEC copy contract)
    - Flat-rate shipping details in clean sections with bg-card panels:
      - "Flat-Rate Shipping" -- $5 flat rate on all orders (or whatever rate the founders decide -- use $5 as placeholder)
      - "Processing Time" -- Ready-to-ship items: 1-2 business days. Made-to-order items: 5-7 business days + 1-2 days shipping.
      - "Delivery" -- Standard USPS shipping, 3-5 business days after processing.
      - "Tracking" -- You'll receive a tracking number by email once your order ships.
    - Each section: heading 14px semibold, body 16px regular text-muted-foreground
    - Sections wrapped in bg-card rounded-lg p-6 panels with 16px gap between panels
    - Page padding: 16px horizontal, max-width 600px centered, 48px top padding

    **Page 4: `src/app/returns/page.tsx` (STORE-07)**

    SEO metadata:
    ```typescript
    export const metadata: Metadata = {
      title: "Returns & Exchanges -- Beads & Bloom",
      description: "Our return and exchange policy for handmade jewelry. We want you to love your purchase.",
    };
    ```

    Content:
    - Heading: "Returns & Exchanges" -- 24px semibold (per UI-SPEC copy contract)
    - Policy sections in bg-card panels:
      - "Our Promise" -- "We want you to absolutely love your jewelry. If something isn't right, we'll make it right."
      - "Returns" -- "If you're not happy with your order, reach out within 14 days of delivery. We'll work with you on a return or exchange."
      - "Made-to-Order Items" -- "Since made-to-order pieces are crafted just for you, we can't accept returns on these unless there's a defect. We'll always fix defects for free!"
      - "How to Start a Return" -- "Just email us or DM us on Instagram and we'll take care of everything."
    - Same panel styling as shipping page
    - Page padding: 16px horizontal, max-width 600px centered, 48px top padding
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/about/page.tsx exists and contains "Our Story" heading
    - src/app/about/page.tsx contains "Making the world more beautiful" mission line
    - src/app/about/page.tsx contains "Every bracelet we sell donates $1 to charity"
    - src/app/about/page.tsx exports metadata with title "Our Story -- Beads & Bloom"
    - src/app/about/page.tsx contains "md:flex" for alternating desktop layout (D-06)
    - src/app/contact/page.tsx exists and contains "Say Hi!" heading
    - src/app/contact/page.tsx contains "instagram.com/beadsandbloom" link
    - src/app/contact/page.tsx contains Mail and Instagram icon imports from lucide-react
    - src/app/contact/page.tsx exports metadata with title "Contact Us -- Beads & Bloom"
    - src/app/shipping/page.tsx exists and contains "Shipping Info" heading
    - src/app/shipping/page.tsx exports metadata with title containing "Shipping"
    - src/app/returns/page.tsx exists and contains "Returns & Exchanges" heading
    - src/app/returns/page.tsx exports metadata with title containing "Returns"
    - None of the 4 pages contain "use client" (all are Server Components)
    - Build completes without errors
  </acceptance_criteria>
  <done>About page renders founder story with alternating layout. Contact page shows email and Instagram DM link. Shipping page explains flat-rate shipping. Returns page explains exchange policy. All four pages have SEO metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Build charity counter and Instagram gallery, wire into homepage</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/components/shared/charity-counter.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shared/instagram-gallery.tsx,
    projects/briefs/beads-and-bloom-website/src/app/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/app/page.tsx,
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    .planning/phases/02-storefront-brand/02-UI-SPEC.md,
    .planning/phases/02-storefront-brand/02-RESEARCH.md
  </read_first>
  <action>
    **IMPORTANT: This task modifies page.tsx which Plan 02 also modifies. If Plan 02 has already run, read the current page.tsx first and ADD to it. If Plan 02 has NOT run yet, the page.tsx will be the placeholder from Plan 01 -- in that case, build a minimal page with just the charity counter and Instagram gallery sections, and Plan 02 will integrate them. The safest approach: build CharityCounter and InstagramGallery as standalone components, then append them to whatever page.tsx currently exists.**

    **Component 1: `src/components/shared/charity-counter.tsx` (client component, per D-07, D-08)**
    - `"use client"` directive
    - Props: `{ total: number }` (parsed from string by the parent Server Component)
    - Count-up animation triggered by IntersectionObserver (per UI-SPEC interaction pattern):
      - Observer threshold: 0.3
      - Only animate once (track `hasAnimated` state)
      - Duration: 2 seconds
      - Easing: ease-out cubic -- `1 - Math.pow(1 - progress, 3)`
      - Use `requestAnimationFrame` loop (per research -- no library needed)
    - Display format: `$${displayValue} donated and counting!` (per UI-SPEC copy contract)
    - Number styling: 32px semibold (Display role), `text-primary` (teal accent #3B9B8F)
    - "donated and counting!" text: 16px regular, `text-muted-foreground`
    - Wrapper: centered text, bg-secondary panel, rounded-lg, padding 48px vertical 16px horizontal
    - If total is 0, still animate to 0 and show "$0 donated and counting!" (per research -- honest and fine for a new store)

    **Component 2: `src/components/shared/instagram-gallery.tsx` (server component)**
    - Per BRAND-04: curated static image gallery linking to Instagram profile (no embed widget -- per research recommendation)
    - Section heading: "Follow Our Journey" -- 24px semibold, centered
    - Subtext: "@beadsandbloom on Instagram" -- 14px, text-primary (teal), linked to `https://instagram.com/beadsandbloom`
    - Gallery grid: 2x2 on mobile (grid-cols-2), 3x2 on tablet (md:grid-cols-3), gap 4px
    - Use 6 placeholder image slots (div with bg-secondary rounded-md aspect-square) since we may not have curated Instagram images in Cloudinary yet. Each placeholder shows a subtle camera icon (lucide-react Camera icon, text-muted-foreground/30) centered.
    - If/when real images are available, swap placeholders for CldImage components. Make this easy by using a `GALLERY_IMAGES` const array at the top of the file that can be filled in later.
    - Each image/placeholder links to `https://instagram.com/beadsandbloom` (target="_blank")
    - Section padding: 48px vertical

    **Step 3: Wire CharityCounter and InstagramGallery into homepage (page.tsx)**
    - Read the CURRENT page.tsx before modifying
    - Add `getCharityTotal()` call to the data fetching
    - Parse total: `const charityTotal = parseFloat((await getCharityTotal()).totalDonated)`
    - Add CharityCounter after the product grid section: `<CharityCounter total={charityTotal} />`
    - Add InstagramGallery after the CharityCounter: `<InstagramGallery />`
    - Homepage section order per D-14: Hero -> CategoryTabs -> ProductGrid -> CharityCounter -> InstagramGallery
    - Spacing: 48px (`mt-12`) between each major section
    - Wrap the entire homepage content in max-width container: `<div className="max-w-[1200px] mx-auto px-4">`
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/shared/charity-counter.tsx contains "use client" directive
    - src/components/shared/charity-counter.tsx contains "IntersectionObserver" for scroll-triggered animation
    - src/components/shared/charity-counter.tsx contains "requestAnimationFrame" for smooth animation
    - src/components/shared/charity-counter.tsx contains "donated and counting!" text
    - src/components/shared/charity-counter.tsx renders number with text-primary class (teal accent)
    - src/components/shared/charity-counter.tsx contains duration of 2000 (2 seconds)
    - src/components/shared/instagram-gallery.tsx contains "instagram.com/beadsandbloom" link
    - src/components/shared/instagram-gallery.tsx contains "Follow Our Journey" heading
    - src/components/shared/instagram-gallery.tsx contains "grid-cols-2" for mobile gallery
    - src/app/page.tsx imports and renders CharityCounter
    - src/app/page.tsx imports and renders InstagramGallery
    - src/app/page.tsx calls getCharityTotal() and passes parsed number to CharityCounter
    - Build completes without errors
  </acceptance_criteria>
  <done>Charity counter on homepage animates from 0 to database total when scrolled into view, displaying "$X donated and counting!" in teal accent. Instagram gallery section shows 6 placeholder slots linking to Instagram profile. Both sections wired into homepage below the product grid.</done>
</task>

</tasks>

<verification>
1. `cd projects/briefs/beads-and-bloom-website && node node_modules/next/dist/bin/next build` completes without errors
2. `/about` route renders with "Our Story" heading and alternating sections
3. `/contact` route renders with "Say Hi!" heading and contact methods
4. `/shipping` route renders with shipping policy content
5. `/returns` route renders with return policy content
6. Homepage shows charity counter with animation
7. Homepage shows Instagram gallery section
8. All pages have correct SEO metadata in exports
</verification>

<success_criteria>
- About page tells the founder story with photo-driven alternating layout (BRAND-01)
- Charity counter on homepage shows animated donation total from database (BRAND-02)
- Contact page has email and Instagram DM link (BRAND-03)
- Instagram gallery on homepage links to Instagram profile (BRAND-04)
- Shipping info page accessible with flat-rate details (STORE-06)
- Returns policy page accessible (STORE-07)
- All pages have SEO metadata (STORE-08)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-storefront-brand/02-03-SUMMARY.md`
</output>
