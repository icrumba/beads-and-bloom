---
phase: 02-storefront-brand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/briefs/beads-and-bloom-website/src/app/layout.tsx
  - projects/briefs/beads-and-bloom-website/src/app/globals.css
  - projects/briefs/beads-and-bloom-website/src/components/ui/card.tsx
  - projects/briefs/beads-and-bloom-website/src/components/ui/badge.tsx
  - projects/briefs/beads-and-bloom-website/src/components/ui/tabs.tsx
  - projects/briefs/beads-and-bloom-website/src/components/ui/sheet.tsx
  - projects/briefs/beads-and-bloom-website/src/components/ui/separator.tsx
  - projects/briefs/beads-and-bloom-website/src/components/ui/skeleton.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/footer.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/mobile-nav.tsx
  - projects/briefs/beads-and-bloom-website/src/lib/queries.ts
autonomous: true
requirements: [STORE-01, STORE-02, STORE-10, BRAND-02]

must_haves:
  truths:
    - "Plus Jakarta Sans (400, 600) renders on all pages instead of Geist Sans"
    - "Brand teal accent color (#3B9B8F) appears on interactive elements"
    - "Off-white background (#FAFAF8) replaces pure white on all pages"
    - "Header shows logo left and hamburger icon right on mobile"
    - "Mobile menu slides out with Shop, About, Shipping, Contact links"
    - "Footer shows brand tagline and quick links"
    - "Drizzle queries return products by category and featured flag"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/src/app/layout.tsx"
      provides: "Plus Jakarta Sans font, brand metadata"
      contains: "Plus_Jakarta_Sans"
    - path: "projects/briefs/beads-and-bloom-website/src/app/globals.css"
      provides: "Brand color CSS variable overrides"
      contains: "oklch(0.63 0.09 175)"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx"
      provides: "Minimal header with logo and mobile nav trigger"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shared/footer.tsx"
      provides: "Footer with tagline and links"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shared/mobile-nav.tsx"
      provides: "Sheet-based mobile navigation"
    - path: "projects/briefs/beads-and-bloom-website/src/lib/queries.ts"
      provides: "Drizzle queries for products, featured products, charity total"
      exports: ["getProducts", "getProductBySlug", "getFeaturedProducts", "getCharityTotal"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/shared/header.tsx"
      via: "import and render in body"
      pattern: "import.*Header"
    - from: "src/components/shared/header.tsx"
      to: "src/components/shared/mobile-nav.tsx"
      via: "Sheet trigger in header"
      pattern: "MobileNav"
    - from: "src/lib/queries.ts"
      to: "src/db/schema.ts"
      via: "Drizzle select from products table"
      pattern: "from\\(products\\)"
---

<objective>
Design system foundation, shared layout, and data access layer for the Beads & Bloom storefront.

Purpose: Every page in Phase 2 needs the brand font, colors, header/footer, and product queries. Building this first means Plans 02 and 03 can execute in parallel without duplicating foundation work.

Output: Brand design system (font + colors), shared layout (header + footer + mobile nav), Drizzle query functions, and all shadcn components installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storefront-brand/02-CONTEXT.md
@.planning/phases/02-storefront-brand/02-UI-SPEC.md
@.planning/phases/02-storefront-brand/02-RESEARCH.md

@projects/briefs/beads-and-bloom-website/src/app/layout.tsx
@projects/briefs/beads-and-bloom-website/src/app/globals.css
@projects/briefs/beads-and-bloom-website/src/db/schema.ts
@projects/briefs/beads-and-bloom-website/src/db/index.ts
@projects/briefs/beads-and-bloom-website/src/types/index.ts
@projects/briefs/beads-and-bloom-website/package.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/index.ts:
```typescript
export type Product = InferSelectModel<typeof products>;
export type CharityTotal = InferSelectModel<typeof charityTotals>;
export type Availability = "ready_to_ship" | "made_to_order";
```

From src/db/index.ts:
```typescript
import { drizzle } from "drizzle-orm/neon-http";
export const db = drizzle({ client: sql, schema: { ...schema, ...relations } });
```

From src/db/schema.ts:
```typescript
export const products = pgTable("products", {
  id: serial().primaryKey(),
  name: varchar({ length: 255 }).notNull(),
  slug: varchar({ length: 255 }).notNull().unique(),
  description: text().notNull(),
  price: numeric({ precision: 10, scale: 2 }).notNull(),
  category: varchar({ length: 100 }).notNull(),
  images: jsonb().$type<string[]>().notNull().default([]),
  colors: jsonb().$type<string[]>().notNull().default([]),
  availability: availabilityEnum().notNull().default("ready_to_ship"),
  inStock: boolean().notNull().default(true),
  featured: boolean().notNull().default(false),
  materials: text(),
  careInfo: text(),
  sortOrder: integer().notNull().default(0),
  // ...timestamps
});

export const charityTotals = pgTable("charity_totals", {
  totalDonated: numeric({ precision: 10, scale: 2 }).notNull().default("0"),
  orderCount: integer().notNull().default(0),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components, swap font to Plus Jakarta Sans, apply brand color CSS variables</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    projects/briefs/beads-and-bloom-website/src/app/globals.css,
    projects/briefs/beads-and-bloom-website/src/components/ui/card.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/badge.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/tabs.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/sheet.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/separator.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/skeleton.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    projects/briefs/beads-and-bloom-website/src/app/globals.css,
    projects/briefs/beads-and-bloom-website/package.json,
    .planning/phases/02-storefront-brand/02-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Install shadcn components.**
    Run from the project directory (`projects/briefs/beads-and-bloom-website/`):
    ```bash
    node node_modules/shadcn/dist/bin.js add card badge tabs sheet separator skeleton --yes
    ```
    Use `node node_modules/shadcn/dist/bin.js` instead of `npx` due to Windows path with `&` character.

    **Step 2: Replace Geist Sans with Plus Jakarta Sans in layout.tsx (per D-10).**
    - Remove `Geist` and `Geist_Mono` imports
    - Import `Plus_Jakarta_Sans` from `next/font/google`
    - Configure: `Plus_Jakarta_Sans({ variable: "--font-sans", subsets: ["latin"], weight: ["400", "600"] })`
    - Update body className to use only the Plus Jakarta Sans variable: `${plusJakartaSans.variable} antialiased`
    - Update metadata to: `title: "Beads & Bloom -- Handmade Ocean-Inspired Jewelry"`, `description: "Handmade jewelry by teen twin sisters. Every purchase donates $1 to charity. Sea turtle, starfish, and shell charm bracelets and necklaces."`

    **Step 3: Override brand colors in globals.css `:root` block (per D-09).**
    Replace these specific CSS variable values in the existing `:root` block:
    ```css
    --background: oklch(0.98 0.002 100);       /* #FAFAF8 off-white */
    --foreground: oklch(0.145 0 0);             /* #1A1A1A dark text */
    --card: oklch(0.94 0.012 80);               /* #F0EBE3 sandy warm */
    --card-foreground: oklch(0.145 0 0);
    --primary: oklch(0.63 0.09 175);            /* #3B9B8F teal accent */
    --primary-foreground: oklch(1 0 0);         /* white on accent */
    --secondary: oklch(0.94 0.012 80);          /* same sandy warm */
    --secondary-foreground: oklch(0.145 0 0);
    --muted: oklch(0.95 0.008 80);
    --muted-foreground: oklch(0.48 0 0);        /* #6B6B6B secondary text */
    --accent: oklch(0.94 0.012 80);
    --accent-foreground: oklch(0.145 0 0);
    --border: oklch(0.90 0.008 80);
    --ring: oklch(0.63 0.09 175);               /* teal ring */
    ```
    Keep all other values (destructive, chart-*, radius, sidebar-*, popover-*, input) unchanged.
    Keep the `.dark` block unchanged (not used in Phase 02).

    Also in globals.css `@theme inline` block, remove `--font-mono: var(--font-geist-mono);` and replace with `--font-mono: ui-monospace, monospace;` since Geist Mono is being removed.
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--primary: oklch(0.63 0.09 175)` (teal accent)
    - globals.css contains `--background: oklch(0.98 0.002 100)` (off-white)
    - globals.css contains `--card: oklch(0.94 0.012 80)` (sandy warm)
    - layout.tsx contains `Plus_Jakarta_Sans` import from `next/font/google`
    - layout.tsx contains `weight: ["400", "600"]`
    - layout.tsx does NOT contain `Geist` or `Geist_Mono`
    - layout.tsx metadata title contains "Beads & Bloom"
    - src/components/ui/card.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/tabs.tsx exists
    - src/components/ui/sheet.tsx exists
    - src/components/ui/separator.tsx exists
    - src/components/ui/skeleton.tsx exists
    - Build completes without errors
  </acceptance_criteria>
  <done>Plus Jakarta Sans (400, 600) configured as --font-sans. Brand color palette (teal #3B9B8F primary, off-white #FAFAF8 background, sandy #F0EBE3 cards) applied via CSS variables. All 6 shadcn components installed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build header, footer, mobile nav, and Drizzle product queries</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shared/footer.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shared/mobile-nav.tsx,
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    projects/briefs/beads-and-bloom-website/src/app/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    projects/briefs/beads-and-bloom-website/src/db/schema.ts,
    projects/briefs/beads-and-bloom-website/src/db/index.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/components/ui/sheet.tsx,
    .planning/phases/02-storefront-brand/02-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Create `src/lib/queries.ts` with Drizzle query functions.**
    Export these functions:
    ```typescript
    import { db } from "@/db";
    import { products, charityTotals } from "@/db/schema";
    import { eq, and, asc } from "drizzle-orm";

    export async function getProducts(category?: string) {
      const conditions = [eq(products.inStock, true)];
      if (category && category !== "all") {
        conditions.push(eq(products.category, category as any));
      }
      return db.select().from(products)
        .where(and(...conditions))
        .orderBy(asc(products.sortOrder));
    }

    export async function getProductBySlug(slug: string) {
      const result = await db.select().from(products)
        .where(eq(products.slug, slug))
        .limit(1);
      return result[0] ?? null;
    }

    export async function getFeaturedProducts() {
      return db.select().from(products)
        .where(and(eq(products.featured, true), eq(products.inStock, true)))
        .orderBy(asc(products.sortOrder));
    }

    export async function getCharityTotal() {
      const result = await db.select().from(charityTotals).limit(1);
      return result[0] ?? { totalDonated: "0", orderCount: 0 };
    }
    ```

    **Step 2: Create `src/components/shared/mobile-nav.tsx` (client component, per D-13).**
    - `"use client"` directive at top
    - Uses shadcn Sheet component (slides from right side)
    - Menu items: Shop (href="/"), About (href="/about"), Shipping (href="/shipping"), Contact (href="/contact")
    - Each item uses Next.js Link component
    - Close sheet on item tap (use `onOpenChange` + router or SheetClose)
    - Menu items use 16px font-size, 600 weight, 48px height for touch targets (min 44px per WCAG)
    - Active item indicator uses primary color text (`text-primary`, which is the teal accent)

    **Step 3: Create `src/components/shared/header.tsx` (client component for nav state, per D-12).**
    - `"use client"` directive
    - Minimal layout: logo text "Beads & Bloom" left-aligned, hamburger menu icon right-aligned
    - Logo is a Next.js Link to "/"
    - Logo text: 20px, font-semibold (600 weight)
    - Header height: 56px, NOT sticky (per D-12 -- more screen space for products)
    - Horizontal padding: 16px on mobile
    - Uses lucide-react `Menu` icon (24x24px) for hamburger
    - Renders MobileNav component with Sheet trigger
    - On desktop (>= 768px): show inline nav links (Shop, About, Shipping, Contact) instead of hamburger. Hide hamburger with `md:hidden`, show nav links with `hidden md:flex`.

    **Step 4: Create `src/components/shared/footer.tsx` (server component).**
    - Background: `bg-secondary` (sandy warm #F0EBE3)
    - Tagline: "Handmade with love by twin sisters." (per UI-SPEC copy contract)
    - Quick links: Shop, About, Shipping, Returns, Contact
    - Instagram link: lucide-react Instagram icon + "@beadsandbloom" text linking to `https://instagram.com/beadsandbloom`
    - Copyright: "2026 Beads & Bloom"
    - Padding: 48px top/bottom, 16px horizontal on mobile
    - Links use 14px font-size, `text-muted-foreground`

    **Step 5: Update layout.tsx to include Header and Footer.**
    - Import Header and Footer components
    - Wrap `{children}` between Header and Footer in the body
    - Structure: `<Header /><main className="min-h-screen">{children}</main><Footer />`

    **Step 6: Update `src/app/page.tsx` to be a minimal placeholder.**
    Replace the current placeholder with a simple Server Component that just renders:
    ```tsx
    export default function HomePage() {
      return (
        <div className="px-4 py-12 text-center">
          <h1 className="text-[32px] font-semibold">Beads & Bloom</h1>
          <p className="text-muted-foreground mt-2">Coming soon -- storefront building in progress</p>
        </div>
      );
    }
    ```
    This placeholder will be replaced by Plan 02.
  </action>
  <verify>
    <automated>cd "projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/queries.ts exports getProducts, getProductBySlug, getFeaturedProducts, getCharityTotal
    - src/components/shared/header.tsx exists and contains "Beads & Bloom" logo text
    - src/components/shared/header.tsx contains "use client" directive
    - src/components/shared/header.tsx imports Menu from lucide-react
    - src/components/shared/mobile-nav.tsx exists and contains "use client" directive
    - src/components/shared/mobile-nav.tsx imports Sheet from ui/sheet
    - src/components/shared/mobile-nav.tsx contains links to "/about", "/shipping", "/contact"
    - src/components/shared/footer.tsx exists and contains "Handmade with love by twin sisters"
    - src/components/shared/footer.tsx contains "instagram.com/beadsandbloom"
    - src/app/layout.tsx imports and renders Header and Footer
    - Build completes without errors
  </acceptance_criteria>
  <done>Header with logo + hamburger renders on all pages. Mobile nav slides out with 4 navigation links. Footer shows tagline, quick links, and Instagram. Drizzle query functions ready for product catalog and charity counter. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd projects/briefs/beads-and-bloom-website && node node_modules/next/dist/bin/next build` completes without errors
2. globals.css `:root` contains brand teal (`oklch(0.63 0.09 175)`) and off-white (`oklch(0.98 0.002 100)`)
3. layout.tsx uses Plus Jakarta Sans, not Geist
4. Header, Footer, MobileNav components exist in `src/components/shared/`
5. `src/lib/queries.ts` exports 4 query functions
6. All 6 shadcn components installed in `src/components/ui/`
</verification>

<success_criteria>
- Brand design system fully applied (font, colors, CSS variables)
- Shared layout (header + footer + mobile nav) renders on all pages
- Drizzle query layer ready for product catalog consumption
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-storefront-brand/02-01-SUMMARY.md`
</output>
