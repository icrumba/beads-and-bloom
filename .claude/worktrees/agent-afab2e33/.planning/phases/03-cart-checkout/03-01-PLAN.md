---
phase: 03-cart-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/briefs/beads-and-bloom-website/package.json
  - projects/briefs/beads-and-bloom-website/src/types/index.ts
  - projects/briefs/beads-and-bloom-website/src/lib/cart-store.ts
  - projects/briefs/beads-and-bloom-website/src/lib/use-hydrated-store.ts
  - projects/briefs/beads-and-bloom-website/src/app/layout.tsx
  - projects/briefs/beads-and-bloom-website/src/components/cart/cart-drawer.tsx
  - projects/briefs/beads-and-bloom-website/src/components/cart/cart-icon.tsx
  - projects/briefs/beads-and-bloom-website/src/components/cart/cart-item.tsx
  - projects/briefs/beads-and-bloom-website/src/components/cart/gift-message.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shop/add-to-cart-button.tsx
  - projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx
  - projects/briefs/beads-and-bloom-website/src/app/products/[slug]/page.tsx
autonomous: true
requirements:
  - STORE-03
  - STORE-09
  - STORE-11

must_haves:
  truths:
    - "User can tap Add to Cart on a product and see the cart drawer slide open with the item inside"
    - "User can increase/decrease quantity with +/- buttons or remove an item with a trash icon"
    - "Cart persists across page navigation and page refresh (localStorage)"
    - "Cart icon in header shows item count badge when items are in cart"
    - "User can select custom color swatches on customizable products before adding to cart"
    - "Same product with different color selections appears as separate cart items"
    - "User can type a gift message up to 150 characters in the cart drawer"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/src/lib/cart-store.ts"
      provides: "Zustand cart store with persist middleware"
      exports: ["useCartStore"]
      contains: "persist"
    - path: "projects/briefs/beads-and-bloom-website/src/components/cart/cart-drawer.tsx"
      provides: "Slide-out cart Sheet with item list, gift message, totals"
      contains: "Sheet"
    - path: "projects/briefs/beads-and-bloom-website/src/components/cart/cart-icon.tsx"
      provides: "Header cart icon with count badge"
      contains: "useHydratedStore"
    - path: "projects/briefs/beads-and-bloom-website/src/components/shop/add-to-cart-button.tsx"
      provides: "Client component wrapping Add to Cart with color selection state"
      contains: "useCartStore"
  key_links:
    - from: "src/components/shop/add-to-cart-button.tsx"
      to: "src/lib/cart-store.ts"
      via: "useCartStore().addItem"
      pattern: "useCartStore.*addItem"
    - from: "src/components/cart/cart-icon.tsx"
      to: "src/lib/cart-store.ts"
      via: "useHydratedStore for item count"
      pattern: "useHydratedStore"
    - from: "src/components/cart/cart-drawer.tsx"
      to: "src/lib/cart-store.ts"
      via: "useCartStore for items, removeItem, updateQuantity, giftMessage"
      pattern: "useCartStore"
---

<objective>
Build the complete cart system: Zustand store persisted to localStorage, slide-out cart drawer, interactive color swatches on customizable products, gift message field, and cart icon with count badge in the header.

Purpose: Enables the core shopping experience -- users can add products to cart, customize colors, add gift messages, and manage their selections. This is the prerequisite for the checkout flow in Plan 02.

Output: Working cart UI with state persistence, ready for checkout integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cart-checkout/03-CONTEXT.md
@.planning/phases/03-cart-checkout/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/index.ts:
```typescript
export type Product = InferSelectModel<typeof products>;
export type Address = {
  line1: string;
  line2?: string;
  city: string;
  state: string;
  zip: string;
  country: string;
};
```

From src/db/schema.ts (product fields relevant to cart):
```typescript
// products table has: id (serial), name (varchar), slug (varchar), price (numeric(10,2)),
// images (jsonb string[]), colors (jsonb string[]), customizable (boolean)
```

From src/components/shop/color-swatches.tsx:
```typescript
// COLOR_MAP: Record<string, string> with entries like "Ocean Blue": "#0ea5e9"
// getColor(name: string): string helper
// ColorSwatches({ colors }: { colors: string[] }) -- display-only component
```

From src/components/shared/header.tsx:
```typescript
// "use client" component with logo, nav items, MobileNav
// Cart icon needs to be added next to MobileNav (mobile) and nav (desktop)
```

From src/app/products/[slug]/page.tsx:
```typescript
// Server Component with <Button size="lg" className="w-full md:w-auto">Add to Cart</Button>
// This must be replaced with a client AddToCartButton component that receives product data
```

From src/app/layout.tsx:
```typescript
// RootLayout wraps children with Header + Footer
// Cart drawer (Sheet) and Toaster need to be added here for global access
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages, create Zustand cart store and hydration hook</name>
  <files>
    projects/briefs/beads-and-bloom-website/package.json,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/lib/cart-store.ts,
    projects/briefs/beads-and-bloom-website/src/lib/use-hydrated-store.ts,
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/package.json,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    .planning/phases/03-cart-checkout/03-RESEARCH.md
  </read_first>
  <action>
1. Install packages from the project directory using `--ignore-scripts` (Windows path workaround per Phase 1 pattern):
   ```
   cd projects/briefs/beads-and-bloom-website && npm install zustand@^5.0.12 sonner@^2.0.7 --ignore-scripts
   ```

2. Add `CartItem` type to `src/types/index.ts` (append, do not modify existing types):
   ```typescript
   // Cart types
   export type CartItem = {
     productId: number;
     name: string;
     price: string; // numeric(10,2) stored as string per Phase 1 convention
     quantity: number;
     image: string; // Cloudinary public ID
     slug: string;
     customColors?: string[]; // Only for customizable products
   };
   ```

3. Create `src/lib/cart-store.ts` — Zustand store with persist middleware (per D-03). Store must:
   - Have `items: CartItem[]` and `giftMessage: string` state
   - `addItem(item: Omit<CartItem, 'quantity'>)` — deduplicates by composite key of `productId + sorted customColors` (per Pitfall 5). If match found, increment quantity. Otherwise add with quantity 1.
   - `removeItem(productId: number, customColors?: string[])` — filter out matching item
   - `updateQuantity(productId: number, quantity: number, customColors?: string[])` — set quantity, min 1 (per D-02)
   - `setGiftMessage(message: string)` — truncate to 150 chars (per D-15)
   - `clearCart()` — reset items to [] and giftMessage to ''
   - `totalItems()` — sum of all item quantities
   - `totalPrice()` — sum of parseFloat(price) * quantity for all items
   - Persist config: `{ name: 'beads-bloom-cart' }`
   - Follow the exact pattern from 03-RESEARCH.md Pattern 1

4. Create `src/lib/use-hydrated-store.ts` — hydration-safe hook that returns fallback during SSR, real value after useEffect. Follow exact pattern from 03-RESEARCH.md Pattern 1.

5. Update `src/app/layout.tsx`:
   - Import `Toaster` from `sonner`
   - Add `<Toaster position="top-center" richColors />` inside `<body>` after `<Footer />`
   - This provides toast notifications for "Added to cart" feedback
  </action>
  <verify>
    <automated>cd "C:/Users/kent2/OneDrive/Desktop/Beads & Bloom/agentic-os/projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "zustand" and "sonner" in dependencies
    - src/types/index.ts exports CartItem type with fields: productId, name, price, quantity, image, slug, customColors
    - src/lib/cart-store.ts exports useCartStore with persist middleware using name 'beads-bloom-cart'
    - src/lib/cart-store.ts addItem deduplicates by productId + customColors composite key
    - src/lib/cart-store.ts setGiftMessage truncates at 150 chars via .slice(0, 150)
    - src/lib/use-hydrated-store.ts exports useHydratedStore function with useState + useEffect pattern
    - src/app/layout.tsx includes Toaster from sonner
    - Build passes without errors
  </acceptance_criteria>
  <done>Zustand cart store with persist middleware, hydration-safe hook, CartItem type, and Toaster are all in place. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build cart drawer, cart icon, interactive color swatches, gift message, and wire Add to Cart</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/components/cart/cart-drawer.tsx,
    projects/briefs/beads-and-bloom-website/src/components/cart/cart-icon.tsx,
    projects/briefs/beads-and-bloom-website/src/components/cart/cart-item.tsx,
    projects/briefs/beads-and-bloom-website/src/components/cart/gift-message.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shop/add-to-cart-button.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx,
    projects/briefs/beads-and-bloom-website/src/app/products/[slug]/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/lib/cart-store.ts,
    projects/briefs/beads-and-bloom-website/src/lib/use-hydrated-store.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/components/shop/color-swatches.tsx,
    projects/briefs/beads-and-bloom-website/src/components/shared/header.tsx,
    projects/briefs/beads-and-bloom-website/src/app/products/[slug]/page.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/sheet.tsx,
    projects/briefs/beads-and-bloom-website/src/components/ui/button.tsx,
    .planning/phases/03-cart-checkout/03-CONTEXT.md
  </read_first>
  <action>
**A. Create cart-icon.tsx (per D-04):**
- "use client" component
- Import ShoppingBag from lucide-react, useCartStore, useHydratedStore
- Render a ShoppingBag icon (h-5 w-5) wrapped in a button
- Show item count badge (small teal circle with white text, absolute positioned top-right) using `useHydratedStore(() => useCartStore.getState().totalItems(), 0)` to prevent hydration mismatch
- Badge only renders when count > 0
- onClick opens the cart drawer (pass `onOpenCart` callback prop or use a shared state)
- Use `suppressHydrationWarning` on the badge span as extra safety

**B. Create cart-item.tsx (per D-02):**
- "use client" component receiving a CartItem
- Display: product image (CldImage, w-16 h-16), name, price, custom colors if any (small colored dots)
- Quantity controls: Minus button, quantity number, Plus button
- Minus at quantity 1 shows Trash2 icon instead (per D-02) — clicking removes item via `useCartStore().removeItem(productId, customColors)`
- Plus increments via `useCartStore().updateQuantity(productId, quantity + 1, customColors)`
- Line total: `$${(parseFloat(price) * quantity).toFixed(2)}`

**C. Create gift-message.tsx (per D-15):**
- "use client" component
- Textarea with label "Add a gift message (optional)"
- 150 character limit with character counter showing `{length}/150`
- Uses `useCartStore().giftMessage` and `useCartStore().setGiftMessage`
- Placeholder: "Write a personal note for the recipient..."

**D. Create cart-drawer.tsx (per D-01):**
- "use client" component using shadcn Sheet (reuses same pattern as mobile-nav)
- Sheet opens from right side (`side="right"`)
- Controlled open state via props (`open`, `onOpenChange`)
- Header: "Your Cart" title with item count
- Body: List of CartItem components. If cart is empty, show friendly empty state: "Your cart is empty" with a shell emoji and "Start browsing" link to "/"
- Below items: GiftMessage component
- Footer: Order summary showing subtotal, flat-rate shipping ($5.00 per D-07), total. "Proceed to Checkout" Button linking to `/checkout`. Disable button if cart is empty.
- Show "Free shipping on orders over $25" message if subtotal >= 25, otherwise show "$5.00 flat-rate shipping"

**E. Update color-swatches.tsx (per D-10, D-11):**
- Add optional props: `interactive?: boolean`, `selected?: string[]`, `onSelect?: (colors: string[]) => void`
- When `interactive` is false (default) or not provided: render exactly as before (display-only)
- When `interactive` is true: swatches are clickable, clicking toggles selection (ring-2 ring-primary around selected), calls `onSelect` with updated selection array
- Export the `COLOR_MAP` so add-to-cart-button can reference it

**F. Create add-to-cart-button.tsx:**
- "use client" component receiving product data: `{ productId, name, price, image, slug, colors, customizable }`
- If `customizable` is true and `colors.length > 0`: render interactive ColorSwatches above the Add to Cart button, track `selectedColors` in local state
- If `customizable` is false: no color selection (per D-11)
- On "Add to Cart" click: call `useCartStore().addItem({ productId, name, price, image: images[0], slug, customColors: selectedColors })`, show toast via `toast.success('Added to cart!')` from sonner, open cart drawer
- For the cart drawer opening: use a global state or event — simplest approach is to co-locate a CartDrawer at layout level with a Zustand `isCartOpen` boolean, or pass via context. Use the cart store itself: add `isOpen: boolean`, `openCart()`, `closeCart()` to the cart store.

**G. Update header.tsx (per D-04):**
- Import CartIcon and CartDrawer
- Add CartIcon next to MobileNav on mobile, and next to desktop nav
- Add CartDrawer component (rendered once in header, opened/closed via cart store)
- CartIcon onClick calls `useCartStore().openCart()`

**H. Update products/[slug]/page.tsx:**
- The product detail page is a Server Component. Replace the static `<Button>Add to Cart</Button>` with the new `<AddToCartButton>` client component.
- Pass product data as props: `productId={product.id} name={product.name} price={product.price} image={product.images[0] || ''} slug={product.slug} colors={product.colors} customizable={product.customizable}`
- The interactive color swatches (if customizable) replace the existing display-only `<ColorSwatches>` in that section. Remove the old `<ColorSwatches>` usage and let `AddToCartButton` handle color display for both customizable and non-customizable products.

**IMPORTANT:** Update the cart store (cart-store.ts) to add `isOpen: boolean`, `openCart: () => void`, `closeCart: () => void` for cart drawer state management. These go OUTSIDE the persist config (cart open state should not persist to localStorage).
  </action>
  <verify>
    <automated>cd "C:/Users/kent2/OneDrive/Desktop/Beads & Bloom/agentic-os/projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/cart/cart-drawer.tsx uses Sheet from shadcn with side="right"
    - src/components/cart/cart-drawer.tsx shows empty state when no items
    - src/components/cart/cart-drawer.tsx shows subtotal, $5.00 shipping, and total
    - src/components/cart/cart-drawer.tsx has "Proceed to Checkout" link to /checkout
    - src/components/cart/cart-icon.tsx shows ShoppingBag icon with count badge from useHydratedStore
    - src/components/cart/cart-item.tsx has +/- buttons with Trash2 icon at quantity 1
    - src/components/cart/gift-message.tsx has textarea with 150 char limit and counter
    - src/components/shop/color-swatches.tsx accepts interactive, selected, onSelect props
    - src/components/shop/add-to-cart-button.tsx is "use client" and calls useCartStore().addItem
    - src/components/shop/add-to-cart-button.tsx shows toast.success on add
    - src/components/shared/header.tsx includes CartIcon component
    - src/app/products/[slug]/page.tsx uses AddToCartButton instead of static Button
    - src/lib/cart-store.ts has isOpen, openCart, closeCart (not persisted)
    - Build passes without errors
  </acceptance_criteria>
  <done>Complete cart system working: slide-out drawer, quantity controls, color selection on customizable products, gift message, cart icon with badge in header, Add to Cart wired on product pages. Build passes.</done>
</task>

</tasks>

<verification>
1. Build passes: `node node_modules/next/dist/bin/next build` completes without errors
2. Cart store exports all expected functions and persists to localStorage with key 'beads-bloom-cart'
3. All cart components exist and import correctly from cart-store
4. Product detail page renders AddToCartButton as a client component
5. Header shows cart icon
</verification>

<success_criteria>
- User can add a product to cart from the product detail page
- Cart drawer slides out showing the added item with quantity controls
- Cart persists across page navigation (Zustand + localStorage)
- Customizable products show interactive color swatches; non-customizable show display-only
- Gift message field accepts up to 150 characters
- Cart icon in header shows item count badge
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-cart-checkout/03-01-SUMMARY.md`
</output>
