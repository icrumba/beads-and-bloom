---
phase: 03-cart-checkout
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - projects/briefs/beads-and-bloom-website/package.json
  - projects/briefs/beads-and-bloom-website/src/lib/stripe.ts
  - projects/briefs/beads-and-bloom-website/src/actions/stripe.ts
  - projects/briefs/beads-and-bloom-website/src/app/checkout/page.tsx
  - projects/briefs/beads-and-bloom-website/src/components/checkout/checkout-form.tsx
  - projects/briefs/beads-and-bloom-website/src/components/checkout/stripe-checkout.tsx
  - projects/briefs/beads-and-bloom-website/src/app/order/confirmation/page.tsx
autonomous: true
requirements:
  - STORE-04
  - STORE-12

must_haves:
  truths:
    - "User can navigate to /checkout from the cart drawer and see a customer info form"
    - "User fills in name, email, shipping address and proceeds to Stripe Embedded Checkout on the same page"
    - "Stripe Embedded Checkout appears embedded on the page (not a redirect) per D-05"
    - "After payment, user is redirected to /order/confirmation?session_id={id} with order summary"
    - "Confirmation page shows: thank-you message, handwritten note mention, $1 charity donation mention per D-13"
    - "Flat-rate $5 shipping is included as a Stripe line item per D-07"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/src/lib/stripe.ts"
      provides: "Stripe singleton instance"
      exports: ["stripe"]
      contains: "new Stripe"
    - path: "projects/briefs/beads-and-bloom-website/src/actions/stripe.ts"
      provides: "Server Action for checkout session creation"
      contains: "use server"
    - path: "projects/briefs/beads-and-bloom-website/src/app/checkout/page.tsx"
      provides: "Checkout page with form and embedded Stripe"
      contains: "checkout"
    - path: "projects/briefs/beads-and-bloom-website/src/app/order/confirmation/page.tsx"
      provides: "Post-payment confirmation page"
      contains: "handwritten thank-you note"
  key_links:
    - from: "src/components/checkout/checkout-form.tsx"
      to: "src/actions/stripe.ts"
      via: "createCheckoutSession Server Action"
      pattern: "createCheckoutSession"
    - from: "src/actions/stripe.ts"
      to: "src/lib/stripe.ts"
      via: "stripe.checkout.sessions.create"
      pattern: "stripe\\.checkout\\.sessions\\.create"
    - from: "src/app/order/confirmation/page.tsx"
      to: "src/lib/stripe.ts"
      via: "stripe.checkout.sessions.retrieve"
      pattern: "sessions\\.retrieve"
---

<objective>
Build the Stripe checkout flow: customer info form, Stripe Embedded Checkout payment, and order confirmation page. Users go from cart to payment to a thank-you page with order details and charity mention.

Purpose: This is the revenue path -- converting cart items into paid orders. Stripe Embedded Checkout keeps customers on the Beads & Bloom domain for trust.

Output: Working checkout page with Stripe integration and confirmation page. Webhook and email are in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cart-checkout/03-CONTEXT.md
@.planning/phases/03-cart-checkout/03-RESEARCH.md
@.planning/phases/03-cart-checkout/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (cart store) -->
From src/lib/cart-store.ts:
```typescript
export type CartItem = {
  productId: number;
  name: string;
  price: string;
  quantity: number;
  image: string;
  slug: string;
  customColors?: string[];
};

// useCartStore provides: items, giftMessage, clearCart(), totalPrice(), totalItems()
```

From src/types/index.ts:
```typescript
export type Address = {
  line1: string;
  line2?: string;
  city: string;
  state: string;
  zip: string;
  country: string;
};
```

From src/db/schema.ts:
```typescript
// orders table: stripeSessionId (varchar unique), customerId, status, totalAmount, shippingAddress (jsonb), giftMessage
// products table: id, name, price (numeric 10,2), slug
```

From src/lib/queries.ts:
```typescript
export async function getProductBySlug(slug: string): Promise<Product | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Stripe packages, create Stripe singleton and Server Action for checkout session</name>
  <files>
    projects/briefs/beads-and-bloom-website/package.json,
    projects/briefs/beads-and-bloom-website/src/lib/stripe.ts,
    projects/briefs/beads-and-bloom-website/src/actions/stripe.ts
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/package.json,
    projects/briefs/beads-and-bloom-website/src/lib/queries.ts,
    projects/briefs/beads-and-bloom-website/src/db/schema.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    .planning/phases/03-cart-checkout/03-RESEARCH.md
  </read_first>
  <action>
1. Install Stripe packages from the project directory:
   ```
   cd projects/briefs/beads-and-bloom-website && npm install stripe@^21.0.1 @stripe/stripe-js@^9.0.0 @stripe/react-stripe-js@^6.0.0 --ignore-scripts
   ```

2. Create `src/lib/stripe.ts` — Stripe server-side singleton:
   ```typescript
   import Stripe from 'stripe';

   export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
     typescript: true,
   });
   ```

3. Create `src/actions/stripe.ts` — Server Action for checkout session creation (per D-09, not an API route):
   - Mark with `'use server'` at top
   - Export `createCheckoutSession` function accepting:
     ```typescript
     {
       items: { productId: number; name: string; price: string; quantity: number; customColors?: string[] }[];
       customerEmail: string;
       customerName: string;
       customerPhone?: string;
       shippingAddress: Address;
       giftMessage?: string;
     }
     ```
   - Validate inputs with zod: email required, name required (3+ chars), address fields required (line1, city, state, zip non-empty)
   - **CRITICAL: Look up product prices server-side** from the database (per anti-pattern in RESEARCH.md). Do NOT trust client-sent prices. Use `db.select().from(products).where(inArray(products.id, itemIds))` to get real prices. Map cart items to their DB prices.
   - Build Stripe `line_items` array: each item gets `price_data` with `currency: 'usd'`, `product_data: { name }`, `unit_amount: Math.round(parseFloat(dbPrice) * 100)` (Stripe uses cents, per Pitfall 3)
   - Add flat-rate shipping line item: `{ price_data: { currency: 'usd', product_data: { name: 'Flat-rate shipping' }, unit_amount: 500 }, quantity: 1 }` (per D-07)
   - Store metadata on the session: `gift_message` (string), `customer_name`, `customer_phone`, `shipping_address` (JSON stringified Address). Also store cart item details (productId, quantity, customColors) as `cart_items` JSON in metadata.
   - Set `return_url` to `${process.env.NEXT_PUBLIC_BASE_URL}/order/confirmation?session_id={CHECKOUT_SESSION_ID}`
   - Set `ui_mode: 'embedded'`, `mode: 'payment'`, `customer_email: customerEmail`
   - Return `session.client_secret!`
   - On error, throw with a descriptive message
  </action>
  <verify>
    <automated>cd "C:/Users/kent2/OneDrive/Desktop/Beads & Bloom/agentic-os/projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "stripe", "@stripe/stripe-js", "@stripe/react-stripe-js"
    - src/lib/stripe.ts exports stripe instance created with process.env.STRIPE_SECRET_KEY
    - src/actions/stripe.ts has 'use server' directive at top of file
    - src/actions/stripe.ts exports createCheckoutSession function
    - src/actions/stripe.ts validates input with zod before creating session
    - src/actions/stripe.ts looks up product prices from database (not trusting client prices)
    - src/actions/stripe.ts adds flat-rate shipping at unit_amount: 500 (cents)
    - src/actions/stripe.ts sets ui_mode: 'embedded' and return_url with {CHECKOUT_SESSION_ID}
    - Build passes without errors
  </acceptance_criteria>
  <done>Stripe SDK installed, server singleton created, Server Action validates input, looks up DB prices, creates embedded checkout session with flat-rate shipping.</done>
</task>

<task type="auto">
  <name>Task 2: Build checkout page with customer form and Stripe Embedded Checkout, plus confirmation page</name>
  <files>
    projects/briefs/beads-and-bloom-website/src/app/checkout/page.tsx,
    projects/briefs/beads-and-bloom-website/src/components/checkout/checkout-form.tsx,
    projects/briefs/beads-and-bloom-website/src/components/checkout/stripe-checkout.tsx,
    projects/briefs/beads-and-bloom-website/src/app/order/confirmation/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/src/actions/stripe.ts,
    projects/briefs/beads-and-bloom-website/src/lib/cart-store.ts,
    projects/briefs/beads-and-bloom-website/src/lib/stripe.ts,
    projects/briefs/beads-and-bloom-website/src/types/index.ts,
    projects/briefs/beads-and-bloom-website/src/components/ui/button.tsx,
    .planning/phases/03-cart-checkout/03-CONTEXT.md
  </read_first>
  <action>
**A. Create checkout-form.tsx (per D-06):**
- "use client" component
- Collects customer info BEFORE Stripe payment (per D-06):
  - Full name (required, text input)
  - Email (required, email input)
  - Phone (optional, tel input)
  - Shipping address: Street address / Line 1 (required), Apt/Suite / Line 2 (optional), City (required), State (required, text input), ZIP code (required)
- All fields validated with zod on submit: email format, name 2+ chars, line1/city/state/zip non-empty
- Show inline validation errors below each field (red text, per Claude's discretion on error UX)
- On valid submit: show order summary (items from cart store, subtotal, $5 shipping, total), then call `createCheckoutSession` Server Action and pass result to `onClientSecret` callback
- Use a two-step UI: Step 1 shows the form, Step 2 shows order summary + Stripe Embedded Checkout
- Loading state: show a spinner on the "Continue to Payment" button while Server Action runs (per Claude's discretion)

**B. Create stripe-checkout.tsx (per D-05):**
- "use client" component
- Import `loadStripe` from `@stripe/stripe-js`, `EmbeddedCheckoutProvider` and `EmbeddedCheckout` from `@stripe/react-stripe-js`
- Initialize stripe with `loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!)` at module level (not in useEffect, per State of the Art in RESEARCH.md)
- Accept `clientSecret: string` as prop
- Render `EmbeddedCheckoutProvider` with `stripe={stripePromise}` and `options={{ clientSecret }}`
- Inside, render `<EmbeddedCheckout />`

**C. Create checkout/page.tsx:**
- "use client" page (needs cart state)
- Import CheckoutForm and StripeCheckout
- If cart is empty (check with useHydratedStore), redirect to "/" or show "Your cart is empty" message with link back to shop
- Render page title "Checkout" with a back link to cart/shop
- Two-phase render: initially show CheckoutForm, once clientSecret is obtained show StripeCheckout
- Layout: max-w-[600px] centered, clean checkout look

**D. Create order/confirmation/page.tsx (per D-12, D-13):**
- Server Component (fetches session data on server)
- Read `session_id` from searchParams
- If no session_id, redirect to "/"
- Call `stripe.checkout.sessions.retrieve(session_id)` to get session details
- Call `stripe.checkout.sessions.listLineItems(session_id)` to get purchased items
- Display:
  - Heading: "Thank you! Your order is on its way." (per D-13)
  - Green checkmark icon (CheckCircle from lucide-react)
  - Order items list with names and quantities
  - Shipping address (from session metadata)
  - Order total
  - "Every order includes a handwritten thank-you note." in a styled callout/card (per D-13)
  - "$1 from your purchase is being donated to The Storehouse." in a styled callout/card (per D-13)
  - Estimated delivery: "Ready to ship" items: 3-5 business days, "Made to order" items: 7-10 business days (display general "5-10 business days" estimate)
  - "Continue Shopping" button linking to "/"
- Style: centered layout, warm and personal tone matching the teen brand
  </action>
  <verify>
    <automated>cd "C:/Users/kent2/OneDrive/Desktop/Beads & Bloom/agentic-os/projects/briefs/beads-and-bloom-website" && node node_modules/next/dist/bin/next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/checkout/checkout-form.tsx is "use client" with fields: name, email, phone (optional), line1, line2 (optional), city, state, zip
    - src/components/checkout/checkout-form.tsx validates with zod before submitting
    - src/components/checkout/stripe-checkout.tsx uses EmbeddedCheckoutProvider with clientSecret prop
    - src/components/checkout/stripe-checkout.tsx loads stripe at module level via loadStripe
    - src/app/checkout/page.tsx is "use client" and shows empty cart message when no items
    - src/app/checkout/page.tsx renders CheckoutForm then StripeCheckout after clientSecret obtained
    - src/app/order/confirmation/page.tsx reads session_id from searchParams
    - src/app/order/confirmation/page.tsx calls stripe.checkout.sessions.retrieve
    - src/app/order/confirmation/page.tsx contains text "handwritten thank-you note"
    - src/app/order/confirmation/page.tsx contains text "donated to The Storehouse"
    - src/app/order/confirmation/page.tsx contains text "Thank you"
    - Build passes without errors
  </acceptance_criteria>
  <done>Complete checkout flow: customer form with validation, Stripe Embedded Checkout on same page, and confirmation page with order summary, thank-you note mention, and charity donation mention. Build passes.</done>
</task>

</tasks>

<verification>
1. Build passes: `node node_modules/next/dist/bin/next build` completes without errors
2. Checkout page exists at /checkout and shows customer form
3. Server Action creates Stripe session with embedded mode and flat-rate shipping
4. Confirmation page at /order/confirmation shows thank-you copy per D-13
5. Server Action validates inputs and looks up DB prices (not trusting client)
</verification>

<success_criteria>
- Checkout page renders customer info form and transitions to Stripe Embedded Checkout
- Stripe session includes flat-rate $5 shipping as a line item
- Server Action validates all required fields before creating session
- Confirmation page displays order summary with thank-you note and charity donation mentions
- All product prices verified server-side from database
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-cart-checkout/03-02-SUMMARY.md`
</output>
