---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/briefs/beads-and-bloom-website/package.json
  - projects/briefs/beads-and-bloom-website/next.config.ts
  - projects/briefs/beads-and-bloom-website/tsconfig.json
  - projects/briefs/beads-and-bloom-website/drizzle.config.ts
  - projects/briefs/beads-and-bloom-website/.env.local.example
  - projects/briefs/beads-and-bloom-website/src/app/layout.tsx
  - projects/briefs/beads-and-bloom-website/src/app/page.tsx
  - projects/briefs/beads-and-bloom-website/src/lib/utils.ts
autonomous: true
requirements: []
user_setup:
  - service: neon
    why: "Database hosting for all product, order, and customer data"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console (https://console.neon.tech) -> Create project -> Connection Details -> Connection string (with ?sslmode=require)"
    dashboard_config:
      - task: "Create a Neon project"
        location: "https://console.neon.tech -> New Project -> name it 'beads-and-bloom'"
  - service: cloudinary
    why: "Product image hosting and optimization"
    env_vars:
      - name: NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME
        source: "Cloudinary Dashboard (https://console.cloudinary.com) -> Dashboard -> Cloud Name"
      - name: CLOUDINARY_API_KEY
        source: "Cloudinary Dashboard -> Settings -> API Keys"
      - name: CLOUDINARY_API_SECRET
        source: "Cloudinary Dashboard -> Settings -> API Keys"
    dashboard_config:
      - task: "Create a free Cloudinary account"
        location: "https://cloudinary.com/users/register_free"

must_haves:
  truths:
    - "Next.js 15 dev server starts without errors in the project directory"
    - "Tailwind CSS 4 is active (CSS-first config with @import tailwindcss)"
    - "shadcn/ui is initialized and the cn() utility function exists"
    - "Drizzle Kit config file points to the schema and uses postgresql dialect"
    - "No package.json or node_modules exist at the agentic-os root"
  artifacts:
    - path: "projects/briefs/beads-and-bloom-website/package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "projects/briefs/beads-and-bloom-website/drizzle.config.ts"
      provides: "Drizzle Kit migration configuration"
      contains: "defineConfig"
    - path: "projects/briefs/beads-and-bloom-website/src/lib/utils.ts"
      provides: "shadcn/ui cn() utility"
      contains: "clsx"
    - path: "projects/briefs/beads-and-bloom-website/.env.local.example"
      provides: "Environment variable template"
      contains: "DATABASE_URL"
  key_links:
    - from: "drizzle.config.ts"
      to: "src/db/schema.ts"
      via: "schema path reference"
      pattern: 'schema.*src/db/schema'
    - from: "package.json"
      to: "drizzle-kit"
      via: "db:push script"
      pattern: "db:push"
---

<objective>
Scaffold the Next.js 15 project with all Phase 1 dependencies installed and configured inside the agentic-os project containment boundary.

Purpose: Every subsequent phase builds on this scaffold. Getting the project structure, dependencies, and configuration right from day one prevents cascading issues.
Output: A running Next.js 15 dev server with Tailwind CSS 4, shadcn/ui initialized, Drizzle ORM installed, and all npm scripts configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and install all Phase 1 dependencies</name>
  <files>
    projects/briefs/beads-and-bloom-website/package.json,
    projects/briefs/beads-and-bloom-website/next.config.ts,
    projects/briefs/beads-and-bloom-website/tsconfig.json,
    projects/briefs/beads-and-bloom-website/src/app/layout.tsx,
    projects/briefs/beads-and-bloom-website/src/app/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (installation commands and version pins)
  </read_first>
  <action>
    1. Create the project directory:
       ```
       mkdir -p projects/briefs/beads-and-bloom-website
       ```

    2. Scaffold Next.js 15 from INSIDE the project directory (critical -- never from agentic-os root):
       ```
       cd projects/briefs/beads-and-bloom-website
       npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --no-import-alias
       ```
       IMPORTANT: Use `create-next-app@15` (NOT `@latest`) to pin to Next.js 15.x. Using `@latest` would install Next.js 16.x.
       Accept all defaults when prompted.

    3. Initialize shadcn/ui (from inside project directory):
       ```
       npx shadcn@latest init
       ```
       Select: New York style, Zinc base color, CSS variables yes.

    4. Install database dependencies:
       ```
       npm install drizzle-orm @neondatabase/serverless
       npm install -D drizzle-kit tsx
       ```
       Do NOT install `pg` (node-postgres) -- it does not work on edge/serverless.

    5. Install remaining Phase 1 deps:
       ```
       npm install next-cloudinary zod dotenv
       ```

    6. Add database scripts to package.json scripts section (merge with existing scripts):
       ```json
       "db:push": "drizzle-kit push",
       "db:generate": "drizzle-kit generate",
       "db:migrate": "drizzle-kit migrate",
       "db:seed": "tsx scripts/seed.ts",
       "db:studio": "drizzle-kit studio"
       ```

    7. Verify package.json shows `"next": "15.x.x"` (NOT 16.x). If it shows 16.x, the wrong scaffold command was used.

    8. Verify NO package.json or node_modules exist at the agentic-os root directory. Only inside projects/briefs/beads-and-bloom-website/.
  </action>
  <verify>
    <automated>cd projects/briefs/beads-and-bloom-website && node -e "const p=require('./package.json'); const v=p.dependencies.next; if(!v.startsWith('1') || parseInt(v.split('.')[0])>=16) {console.error('Wrong Next.js version:',v);process.exit(1)} console.log('Next.js version OK:',v)" && test ! -f ../../../package.json && echo "No root package.json - OK"</automated>
  </verify>
  <acceptance_criteria>
    - projects/briefs/beads-and-bloom-website/package.json contains `"next": "15.` (not 16.x)
    - projects/briefs/beads-and-bloom-website/package.json contains `"drizzle-orm"` in dependencies
    - projects/briefs/beads-and-bloom-website/package.json contains `"@neondatabase/serverless"` in dependencies
    - projects/briefs/beads-and-bloom-website/package.json contains `"next-cloudinary"` in dependencies
    - projects/briefs/beads-and-bloom-website/package.json contains `"zod"` in dependencies
    - projects/briefs/beads-and-bloom-website/package.json contains `"db:push"` in scripts
    - projects/briefs/beads-and-bloom-website/package.json contains `"db:seed"` in scripts
    - projects/briefs/beads-and-bloom-website/package.json does NOT contain `"pg"` in dependencies
    - No package.json exists at the agentic-os root
    - projects/briefs/beads-and-bloom-website/node_modules/ exists
    - projects/briefs/beads-and-bloom-website/src/app/layout.tsx exists
    - projects/briefs/beads-and-bloom-website/src/app/page.tsx exists
  </acceptance_criteria>
  <done>Next.js 15 project scaffolded with all Phase 1 dependencies installed. Package.json confirms Next.js 15.x, Drizzle ORM, Neon serverless driver, next-cloudinary, zod, and all db scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Drizzle, environment template, and verify dev server starts</name>
  <files>
    projects/briefs/beads-and-bloom-website/drizzle.config.ts,
    projects/briefs/beads-and-bloom-website/.env.local.example,
    projects/briefs/beads-and-bloom-website/src/db/index.ts,
    projects/briefs/beads-and-bloom-website/src/app/page.tsx
  </files>
  <read_first>
    projects/briefs/beads-and-bloom-website/package.json (verify deps installed),
    projects/briefs/beads-and-bloom-website/tsconfig.json (verify path aliases),
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 and Pattern 3 examples)
  </read_first>
  <action>
    1. Create `drizzle.config.ts` at project root:
       ```typescript
       import "dotenv/config";
       import { defineConfig } from "drizzle-kit";

       export default defineConfig({
         out: "./drizzle",
         schema: "./src/db/schema.ts",
         dialect: "postgresql",
         dbCredentials: {
           url: process.env.DATABASE_URL!,
         },
       });
       ```

    2. Create `src/db/index.ts` (database client singleton):
       ```typescript
       import { drizzle } from "drizzle-orm/neon-http";
       import { neon } from "@neondatabase/serverless";
       import * as schema from "./schema";

       const sql = neon(process.env.DATABASE_URL!);
       export const db = drizzle({ client: sql, schema });
       ```
       Note: This file will error until schema.ts exists (created in Plan 02). That is expected.

    3. Create `.env.local.example` (template for required env vars):
       ```
       # Database (Neon Postgres)
       # Get from: https://console.neon.tech -> Project -> Connection Details
       DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

       # Cloudinary (Product images)
       # Get from: https://console.cloudinary.com -> Dashboard
       NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

       # Cloudinary API (for seed script image upload only)
       CLOUDINARY_API_KEY=your-api-key
       CLOUDINARY_API_SECRET=your-api-secret
       ```

    4. Update `src/app/page.tsx` to a minimal placeholder that confirms the app is running:
       ```tsx
       export default function Home() {
         return (
           <main className="flex min-h-screen flex-col items-center justify-center p-8">
             <h1 className="text-4xl font-bold">Beads &amp; Bloom</h1>
             <p className="mt-4 text-lg text-muted-foreground">
               Handmade ocean-inspired jewelry
             </p>
             <p className="mt-2 text-sm text-muted-foreground">
               Coming soon...
             </p>
           </main>
         );
       }
       ```

    5. Create the `scripts/` directory for the seed script entry point (created in Plan 03):
       ```
       mkdir -p projects/briefs/beads-and-bloom-website/scripts
       ```

    6. Start the dev server to verify everything works:
       ```
       cd projects/briefs/beads-and-bloom-website && npm run dev
       ```
       Verify it starts without errors on http://localhost:3000. Kill the dev server after confirming.

    7. Run a build to catch any type errors:
       ```
       cd projects/briefs/beads-and-bloom-website && npm run build
       ```
       Note: Build may warn about src/db/index.ts importing non-existent schema.ts -- this is expected and acceptable. If it causes a build failure, temporarily comment out the db/index.ts import until Plan 02 creates the schema.
  </action>
  <verify>
    <automated>cd projects/briefs/beads-and-bloom-website && test -f drizzle.config.ts && test -f .env.local.example && test -f src/db/index.ts && grep -q "defineConfig" drizzle.config.ts && grep -q "DATABASE_URL" .env.local.example && grep -q "neon-http" src/db/index.ts && echo "All config files verified"</automated>
  </verify>
  <acceptance_criteria>
    - projects/briefs/beads-and-bloom-website/drizzle.config.ts contains `defineConfig`
    - projects/briefs/beads-and-bloom-website/drizzle.config.ts contains `schema: "./src/db/schema.ts"`
    - projects/briefs/beads-and-bloom-website/drizzle.config.ts contains `dialect: "postgresql"`
    - projects/briefs/beads-and-bloom-website/src/db/index.ts contains `drizzle-orm/neon-http`
    - projects/briefs/beads-and-bloom-website/src/db/index.ts contains `@neondatabase/serverless`
    - projects/briefs/beads-and-bloom-website/.env.local.example contains `DATABASE_URL=`
    - projects/briefs/beads-and-bloom-website/.env.local.example contains `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=`
    - projects/briefs/beads-and-bloom-website/.env.local.example contains `CLOUDINARY_API_KEY=`
    - projects/briefs/beads-and-bloom-website/src/app/page.tsx contains `Beads`
    - projects/briefs/beads-and-bloom-website/scripts/ directory exists
  </acceptance_criteria>
  <done>Drizzle config points to src/db/schema.ts with postgresql dialect. DB client singleton uses neon-http driver. Env template documents all required variables with signup URLs. Dev server starts and renders the placeholder page.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors from inside projects/briefs/beads-and-bloom-website/
- No package.json, node_modules, or next.config.ts at the agentic-os root
- package.json shows Next.js 15.x (not 16.x)
- drizzle.config.ts references src/db/schema.ts
- .env.local.example has DATABASE_URL and CLOUDINARY vars
</verification>

<success_criteria>
- Next.js 15 dev server starts on localhost:3000 from inside the project directory
- All Phase 1 dependencies installed (drizzle-orm, @neondatabase/serverless, next-cloudinary, zod)
- shadcn/ui initialized with cn() utility in src/lib/utils.ts
- Drizzle config and db client singleton created
- Environment variable template created
- Project containment: all files inside projects/briefs/beads-and-bloom-website/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
