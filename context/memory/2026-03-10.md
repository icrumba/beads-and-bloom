## Session 1

### Goal
Add /usage display to end-of-session wrap-up

### What happened
- Added Step 5 to `meta-wrap-up/SKILL.md` — runs `/usage` after session summary so user sees plan usage and remaining capacity
- Identified bug: wrap-up skill wasn't writing to the daily memory file during the session
- Decision: memory files are per-day (not per-session), with multiple session blocks appended throughout the day
